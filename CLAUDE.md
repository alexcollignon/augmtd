# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

```bash
npm run dev        # start Next.js dev server (port 3000)
npm run build      # production build
npm run lint       # ESLint via next lint
```

No test suite exists. Verification is done by running the dev server and exercising the UI directly.

**Hetzner deployment** (meeting-bot — no CI):

`docker-compose` v1.29 on Hetzner has a `ContainerConfig` bug and cannot recreate containers after a rebuild. Use the manual sequence:
```bash
# 1. Copy changed file(s)
scp infra/meeting-bot/transcription_worker.py root@46.224.176.245:/root/augmtd-infra/infra/meeting-bot/

# 2. Rebuild image on server
ssh root@46.224.176.245 "cd /root/augmtd-infra/infra/hetzner && docker build -t hetzner_meeting-bot:latest ../meeting-bot"

# 3. Replace running container (.env is at /root/augmtd/.env)
# IMPORTANT: docker-compose.yml maps env var names (e.g. NEXT_PUBLIC_SUPABASE_URL → SUPABASE_URL,
# MEETING_BOT_SECRET → BOT_SECRET). The manual docker run must pass these explicitly or they must
# exist under their Python names in /root/augmtd/.env. As of June 2026, /root/augmtd/.env has
# BOT_SECRET and SUPABASE_URL appended directly — do not remove them.
ssh root@46.224.176.245 "docker stop hetzner_meeting-bot_1; docker rm hetzner_meeting-bot_1; docker run -d --name hetzner_meeting-bot_1 --restart unless-stopped --shm-size=2gb --dns 8.8.8.8 --dns 8.8.4.4 -p 3001:3001 -v hetzner_bot-scheduler:/data -v /root/augmtd-infra/google-auth.json:/app/google-auth.json:ro --env-file /root/augmtd/.env -e AUGMTD_WEBHOOK_BASE_URL=https://app.augmtd.ai -e MAX_CONCURRENT_BOTS=4 -e WHISPER_SERVICE_URL=http://172.19.0.1:8000 hetzner_meeting-bot:latest"

# 4. Verify
ssh root@46.224.176.245 "docker logs hetzner_meeting-bot_1 --tail 20"
```

For config-only changes (no Python dependency changes), `docker restart` is sufficient if the file is volume-mounted — but `transcription_worker.py` is baked into the image, so a full rebuild is always needed for Python changes.

**Whisper server** (`augmtd_whisper_1` — note: a DIFFERENT compose project name than the bot's `hetzner_*`): runs `fedirz/faster-whisper-server:latest-cpu` with **`WHISPER__COMPUTE_TYPE=int8` (load-bearing — the unset default silently ran float32: ~2x slower AND ~6GB RSS on 40-min files, the OOM-kill/"Server disconnected" retry loop of Aug 4-5)**. The `WHISPER__MODEL` env is only a default; the WORKER names its model per-request (`deepdml/faster-whisper-large-v3-turbo-ct2` since Aug 5 — benchmarked 2-3x faster than the old medium/fp32 at better accuracy; needs a punctuated `prompt` or turbo emits lowercase unpunctuated text; `language` omitted → per-file auto-detect). ⚠️ The server SILENTLY IGNORES unknown form fields (`condition_on_previous_text` was sent for months and never applied) — check `/openapi.json` before adding params. Models cache in `/root/whisper-cache` (host mount) so recreates don't re-download. Ports bind to `127.0.0.1` + `172.19.0.1` ONLY (the bot reaches it via the docker-bridge gateway; 0.0.0.0 exposed an unauthenticated ASR server to internet scanners). Recreate:
```bash
ssh root@46.224.176.245 "docker stop augmtd_whisper_1; docker rm augmtd_whisper_1; docker run -d --name augmtd_whisper_1 -e WHISPER__COMPUTE_TYPE=int8 -e WHISPER__MODEL=deepdml/faster-whisper-large-v3-turbo-ct2 -p 127.0.0.1:8000:8000 -p 172.19.0.1:8000:8000 -v /root/whisper-cache:/root/.cache/huggingface --memory 5g --restart unless-stopped fedirz/faster-whisper-server:latest-cpu"
```
Speed reality on the CX32 (4 vCPU): turbo-int8 ≈ 0.21-0.25× realtime (a 40-min meeting transcribes in ~9-10 min; was 12-18 min + crashes). CPU-bound at ~3 of 4 cores — parallel chunking won't help much; the next real step-change is incremental transcription during recording or a bigger box.

**Storage upload limits**: the Supabase PROJECT-global limit governs any bucket with `file_size_limit: null` (the `meeting-recordings` bucket predates its 500MB code param — `createBucket` is a no-op on existing buckets; the presign route now also asserts via `updateBucket`). The global limit sat at the 50MB default until Aug 5 (dashboard showed 500MB UNSAVED — always verify with a real oversized PUT, not the UI) → every recording over ~45 min failed with a bare 400 and the audio was lost. Now 500MB global + client-side retry/download fallback + 32kbps mono Opus recording (~14MB/h — was 128kbps/~58MB/h).

**Recording survivability (Aug 5 — a recording must survive its tab AND its server)**: (1) **THE VAULT** (`lib/recording/vault.ts` + `useRecording` write-through): every ~1s chunk mirrors to IndexedDB with a 5s heartbeat; the session is deleted ONLY after the server confirm succeeds; a crash/forced-close/dead-battery loses ≤1s of audio. `RecordingRecoveryBanner` (mounted in RecordingProvider, app-wide) surfaces stale-heartbeat sessions on any later visit — Upload & transcribe (`vaultUploadSession`: presign→PUT→confirm, skips re-upload when only confirm failed) / Download / Discard. All vault ops are best-effort try/catch — Safari private mode must never break recording itself. (2) **Failed uploads are retryable**: the finished blob is held in `pendingUploadRef` until confirm lands; error UI = Retry upload / Download audio / explicit Discard (the old "Dismiss" silently destroyed the audio). beforeunload guards recording AND uploading AND error-with-pending. (3) **THE STUCK-TRANSCRIPTION SWEEP** (`transcription_worker.py` `requeue_stuck_transcriptions`, looped from `main.py` startup + every 30 min): a worker crash/box reboot mid-transcription used to leave `bot_state='processing'` forever ("Transcribing…" with no retry); the sweep requeues rows stuck >30 min with a storage path through the existing transcript_id retry path, sequentially, cap 3/sweep. Transcribing itself is client-disconnect-safe by design (all server-side) — the user can close the laptop the moment the upload completes. Smoke-verified Aug 5 (browser-automated on the dev server): banner render + discard, live chunk mirroring with fresh heartbeat, upload→confirm→vault-clear; encoder A/B on a synthesized signal (~35kbps new vs ~150kbps old — the automated env's mic is DEAD, 0 bytes even on the old code, so a header-only blob in that env is the environment, not a capture bug). One real spoken-mic recording remains for owner validation.

**Database migrations** are SQL files in `supabase/migrations/`. Apply manually in the Supabase dashboard SQL editor — there is no migration runner wired to `npm run dev`. New migrations are not auto-applied.

## Architecture

### THE EXPERIENCE SPEC (`docs/experience-spec.md` — Aug 2, THE CONSTITUTION)

Every surface change, engine law, and copy decision must TRACE to a clause of the experience spec — the one sentence ("a team of competent humans with your full context took over; you manage"), the seat table (each surface owns ONE job), the ten laws (one fact one home · the brief is derived not remembered · asks live and die with their work · speak consequence · deltas not events · never restate the settled · one CTA row · the word is the deed · earned calm · truth before presentation), and the acceptance tests. Written after the Aug 2 fragmentation correction: fix the law's whole class, never the screenshot; one coherent arc over N reactive patches.

### THE RELAY CANVAS ARC (Aug 21–24 — plan `docs/relay-canvas-plan.md` = THE CONSTITUTION; suite `scripts/smoke-relay.ts` **576/576**; W1–W5 + the material lane on dev `c8fb7a3`)

Workflows became a readable relay track (born from a pilot's CV-triage whiteboard; owner calls: keep the building-blocks relay — NEVER a free 2D canvas, THE MACHINE LAYS OUT; Make/n8n recognizability on Zapier's vertical spine; subprocess composition instead of a branching engine; current capabilities only — REGISTRY AS CATALOGUE, integrations later = one row). Laws: ONE SCHEMA FOUR DOORS (describe · coworker chat · Home chat · canvas — a primitive missing any door doesn't ship; the standing parity gate) · MANY DOORS ONE RUN (`workflows.triggers[]` any-of, at most one schedule; migration `20260821_workflows_triggers.sql` APPLIED) · BLOCKS CARRY RECEIPTS (per-block readiness while building) · INPUTS ARE VISIBLE.

- **W1 TRIGGER DOORS**: `lib/workflows/trigger-sources.ts` registry (mail/file/meeting/workflow) + `normalizeTriggers` = THE ONE READER (legacy reaction folds to a mail door); `lib/workflows/reactions.ts` generalized to one judge/one fire/N sources; mail keeps its HISTORICAL exactly-once token `inbox` (changing it re-fires history); the `workflow` door is STRUCTURAL (zero AI) with the SELF-LOOP GUARD; Studio WHEN block (the old editor's DESTROYER BUG — silently overwriting reaction triggers — is dead); ONE authoring home `lib/workflows/author-doors.ts` (by-name resolution ladder, AMBIGUITY IS A REFUSAL, spoken note channels).
- **W2 INPUTS + MATERIAL**: `workflow_inputs` store (item_plans) + WORKS WITH tray; `buildInputsBlock` rides the SYSTEM channel (never previousOutputs — the verify gate must not see it as source); the material door (run POST `{material}`); the file door fires at extraction-complete.
- **W3 SUBPROCESS (law: A SUBPROCESS IS A HANDOFF TO A MACHINE)**: `lib/workflows/subprocess.ts` — the ⧉ station parks the parent via the EXISTING awaiting machinery (insert-first claim carrying THE BATON on the link row; atomic resume claim; stranded sweep; failure propagates spoken); depth cap 1, circular refusal, test never fires the child; loops = composition via the workflow-delivers door. **W3b THE THROTTLE, NEVER A SHREDDER**: per-workflow daily fire limit (`lib/workflows/fire-limit.ts`, default 20, clamp 1–100, NO unlimited) queues-not-drops — deferred fires drain oldest-first with an atomic start claim; the throttle sits BEHIND the judge; counting fact `deferred !== true`.
- **W4 THE CASE LAYER**: A CASE IS AN ENTITY (`lib/workflows/case-step.ts` — machine-founded UNTRACKED, the pinning law holds); per-workflow index → deterministic distinctive-token pre-pass → one classification judge whose key is BOUND to the user's instruction; never-overwrite `entity_links` via the fire-record atom; THE GROUNDING SWAP (`entityRunGrounding` — the run reads its case's accumulated page: cross-run memory, the whole point); A FILED FILE IS A VISIBLE FILE (the link writer stamps `knowledge_files.entity_id` fill-if-empty — the grounding reads documents off that column). Wording stays "File it under its record" (owner: case-as-property/toggle idea recorded, unbuilt).
- **W5 DOOR FILTERS (the 80/20)**: deterministic per-source filters on registry rows — FAIL-CLOSED, AND semantics, gating candidacy BEFORE the judge (filters-only doors fire with ZERO AI); fireability = condition OR filter; PREFER-A-FILTER + THE OMIT RULE in generate-config; topical ≠ structural ("about the analyst role" stays judged).
- **THE MATERIAL LANE (found live: a real application email — the run never saw the attached CV)**: `ReactionEvent.material` carries the event's fuller text into the FIRED run's context only (mail = the sync's already-extracted `attachments[].extractedText`; file = the document's own text; excerpt-marked clips, appended after the head's cap); the judge structurally never reads it — attachment NAMES ride the gist.
- **THE POWER-PROMPT REPAIRS (Aug 25 — a frozen prompt authored outside the product, pasted ONCE; five classes found live, all fixed + retest-proven on the identical prompt; relay 669/669 · frames 228 · processes 136 · run-record 102 · handoffs 250)**: (F1) **THE GATE IS NEVER THE DELIVERABLE** — run-workflow's picker structurally excludes approval/handoff/case outputs by step TYPE (never marker-text matching; verify stays — it returns the corrected draft), after a share-linkable frame ranked EIGHT FABRICATED candidates off an approval marker line; + **THE CONTENT FLOOR REACHES THE FRAME LANE** (`lib/frames/generate-frame.ts` `isThinFrameSource`, `FRAME_MIN_CONTENT_CHARS=200`, marker-shape test) — a thin/marker source refuses IN CODE before any AI client is built. (F2) **THE SCOPING GUARD** — `recognizeWorkflowEntity` candidacy gated by the distinctive-token law (`namesStatedIn` in case-step, ONE implementation shared with `deterministicCaseMatch`); "Service" alone can never bind a workflow to a project (a generic-token auto-scope had made doors silently dead while readiness said ready); registry column `carriesEntity` (file/workflow = false) + readiness rule 9 SPEAKS when a scope makes every door unhearable — visibility only, `runDoors`' fail-closed pre-filter unchanged. (F3) **THE WORKFLOW-LEVEL CASE KEY** — `CaseStep.case_name` (the constant the user stated) beside `case_instruction` (the per-event identity question); `resolveCaseForRun` fully deterministic zero-AI on the stated path; generate-config authors exactly ONE of the two (THE TWO SHAPES rule); Studio two-option toggle, never auto-detect; a CV never names its opening — the constant shape is how single-opening workflows file at all. (F4) **DOORS ARE STRUCTURAL-BROAD** — a door's `when` names the KIND of thing that arrives, never fit-for-a-role (a door refusal is silent; a pipeline mismatch is visible and parked — a real applicant was silently dropped by a topical door); accept_material authors true when material is handed over. (F5) **THE LEDGER CONFIRM CARRIES THE WHOLE DRAFT** — workflows-ledger (its OWN fork of the draft card, not the shared component) spreads `...draft` at both confirm doors (triggers/inputs/fire_limit reach creation), renders doors/docs/pace, and all FIVE note channels render in both surfaces (needs_person_note was typed nowhere). **(F6) THE CLOCK REACHES FRAME CHROME** — a generated frame's footer read "Updated 2024" (the content floor held for candidate facts and leaked in DECORATIVE chrome): `lib/frames/generate-frame.ts` now injects a CODE-COMPUTED today into the prompt assembly (executeAIStep's idiom) beside a FRAME_CONTRACT chrome rule — every date/time/year/period label/"as of"/"last updated" in header, footer, tile, caption or axis must appear in the material or BE the provided today; never derived, never a plausible year; unsure → omit the date entirely (gates FC, frames 235). **(F7) A SETTLED SCORE STAYS SETTLED** — run 2 restated run 1's settled "Experience 10/10 · Overall 9.5" as "9/10 · 9.0" because the case ledger remembered THAT an item arrived and nothing about what was concluded: the case atom now carries its run's `outcome` (`stampAtomOutcome` — resolves structurally via the `run_case` stamp + the `reaction_fire` record, fills/replaces ITS OWN atom only, digest = the run's LAST CONTENT step clipped 500 under the excerpt law), stamped from run-workflow's success tail (never a failed run, never test mode, non-fatal; a parked run stamps when it later succeeds — /resume re-enters runWorkflow on the same runId and reaches the same tail; a content-less pipeline records nothing rather than passing its filing card off as a verdict), and `caseAtomsBlock` serves it beside its item under a header rule: recorded conclusions are SETTLED, restate as recorded, re-derive only when the new arrival genuinely changes the picture and then SAY the score moved and why (gates CA10 + CS source floors, relay 686). ⚠️ SUITE LESSON: the smoke-frames AI fence poisoned `AWS_BEDROCK_ACCESS_KEY_ID/_SECRET_ACCESS_KEY` but the factory reads `AWS_BEDROCK_ACCESS_KEY/_SECRET_KEY` — a fence with the wrong names fenced nothing, latent until the probe hosts moved to bedrock_optimised (tenant_configs mitigation for the Aug 25 dry-OpenAI-key incident, the mirror of Aug 11's).
- **THE ATTENTION WAVE (Aug 25)**: an event-fired run parked at approval raises a REAL deck ask — the handoff-shaped commitment (`raiseRunDecisionAsk` in standing.ts, fired from the UNBOUND branch of narrateApprovalAsk/narrateGuardrailHold; scheduled workflows keep their standing-room narration, never a double row), exactly-once, cleared through the ONE resume door on approve AND reject, orphans swept by the dispatcher. + **THE KICK** (`app/api/internal/runs/kick`, maxDuration 300, AGENTOS_SECRET bearer, base URL AUGMTD_WEBHOOK_BASE_URL): fireReaction dispatches it fire-and-forget in request scope — arrival-to-run in seconds instead of the hourly backstop; ONE shared claim (`claimQueuedEventRun` + `eventRunContext` incl. the subprocess baton) across kick/backstop/drain so they race safely; deferred fires structurally unreachable (the drain owns them).
- **THE PROMPT ADAPTER (Aug 25 — a pilot's ChatGPT-style paste timed out; then a paragraph-shaped paste lost the whole rubric)**: generate-from-description maxDuration 60→300; `detectPromptShape` (deterministic-first — [PASTE…] placeholders / persona openings decisive alone; a narrow ambiguous band pays one 5-token judged read failing safe to machine); THE RUBRIC RIDES VERBATIM BY CODE — the model places `{{USER_RUBRIC}}`, `placeRubric` substitutes the user's words (append-under-header fallback; the sentinel sweep is UNCONDITIONAL); `stripPastePlaceholders` is SPAN-scoped (line-scoped stripping deleted a one-line paste's entire rubric — the suite fixture had newlines, the pilot's paste didn't) + the 60% survival floor (worst case the original stays whole, spoken). THE UNNAMED SINGLE CASE speaks at authoring (code-owned note — "nothing accumulates until it knows what to file under").
- **THE INPUT STATION (Aug 25 — the run stops and asks the PERSON; paste | pin | attach | say it)**: step type `input {ask, accepts}` parks through the SAME awaiting machinery (gate KIND distinguishes in served state; `GATE_WORDS` = the one word table); test mode substitutes a MARKED sample; the deck ask fires for EVERY input park (no standing room can hold a paste box — the stated asymmetry); the resume door takes `{input:{text|kbFileId|pin}}` and REFUSES a bare approve; the supplied material becomes the STATION'S OWN StepOutput so later gates park again by construction; readiness rule 10. **DECLARED INPUTS AUTHOR STATIONS** ("Inputs: 1. X: [PASTE X]" → one station per input, label = the user's words, cap 4 spoken; A HOME IS A THING THAT EXISTS — enforcement reads SANITISED doors, never the model's wish list; with ≥1 station "arrives" needs a real door; the note derives from SEATED steps by exact match). **THE SUPPLY**: ONE shared `components/workflows/input-supply-form.tsx` (paste · pin w/ per-door presumption · attach) mounted in the process drawer's gate card (THE GATE CARRIES ITS DEED — answered in place, never a screen-hop; the loading-flash floor: the fallback card waits for steps AND respects the served gate kind) and the deck deep-dive card (+ feeds/prepared-by + the folded already-in-this-run trail). **ATTACH** = `POST runs/[id]/supply-upload` (4MB, SYNC extraction — text real at return or an honest 422 w/ cleanup; indexes to KB via the shared ingest; **A SUPPLY IS AN ANSWER, NOT AN ARRIVAL** — never fires the door seam, else answering run 1 spawns run 2). **SAYABLE** (parity): `supply_run_input` chat tool sharing ONE implementation with the door (`answerInputStation` — claim/validation literals exist exactly once); ambiguity REFUSES BY LISTING; native + AgentOS passthrough + Python mirror. Run-now on a station-bearing workflow SKIPS the generic material sheet (ONE predicate `asksForMaterial`, station short-circuit first; an unserved fact never claims).
- **THE NAV + PERF WAVES (Aug 25, owner-walk-driven)**: `components/ui/back-link.tsx` — back returns WHERE YOU CAME FROM (module-level soft-nav counter; per-surface fallback; the frame full-screen's literal "Back to Home" died; plain `<a>` full navigations → `<Link>`); "New chat" REMOVED — sidebar Home IS the chat door (same-path click preventDefaults: the click is the reset, no redundant RSC round-trip stealing the caret; focus waits for the paint via double-rAF — first click, every path); Studio's **Save & turn on** (a draft's primary action activates EXPLICITLY — "Adjust in Studio" created drafts the ledger said to "finish in Studio" while Studio had no finish: the dead-end loop); the ledger describe field grows-to-cap (a pasted brief is readable, not a ticker); the workflows surfaces follow the instant-load doctrine (the runs route's ~90-query N+1 → 2 batched via `prefetchDecisionInputs`; routes flattened to 2–3 flights; stamped LS caches so Back paints warm; tabs never refetch what the page holds; the drawer = ONE request via `?run=`; `getWorkspaceFeatures` reads through the cached workspace).
- **⚠️ Open**: AgentOS box redeploy for the newest Python tool vocabulary (mechanical); mini-rail shared component; case-resolver-reads-pinned-list (owner call); frames Phase 4 "talk to frames" (spec first — real pilot pull now); rare timing flake in the TR throttle-drain suite section (one lap in four).

### THE FRAMES ARC (Aug 19–20 — plan `docs/frames-plan.md` = THE CONSTITUTION; suite `scripts/smoke-frames.ts` **185/185**; Phases 1–3 + THE SERIES built in one arc)

Living interactive deliverables (the Dust-Frames / Claude-artifacts class), built on ONE deciding law: **a frame is an ARTIFACT KIND, not a surface** — it enters at THE ONE PRODUCTION DOOR (materialize.ts TIER 0), so Home chat, coworker DMs, delegations and workflow runs all ship frames the day the door can; the workflow deep-dive's Frames tab is a FILTER, never a second machine.

- **THE LOCKED FRAME** (`lib/frames/validate-frame.ts` + ONE renderer `components/frames/frame-card.tsx`): one self-contained HTML file, ZERO egress — 24 reject vectors (incl. `<base>` re-pointing relative URLs, `formaction=`, `ping=`), ALL reasons collected for the one repair pass, CSP meta injected; rendered ONLY via `srcDoc` + `sandbox="allow-scripts"` (opaque origin — `allow-same-origin` exists nowhere; a source gate pins the single sandbox site). Serving is **JSON-only, never text/html on our origin** (same-origin XSS), which is why no separate sandbox domain is needed. Numbers come from the DATA LANE (computeDataFacts) — the "✓ computed in code" chip renders only from the structural provenance stamp, which rides INSIDE `content` (what every caller persists → zero caller edits).
- **THE FRAME SERIES (the Claude-artifacts shape; owner call)**: explicit output `output_config.artifactType: 'frame'` (Studio select · generate-config authors it · chat task tools; FRAME_WORDS survives as the chat fallback only) + ONE stable head id per workflow — `lib/frames/series.ts` `upsertFrameSeries` is THE ONE WRITER (update-in-place; previous generation → `versions[]`, cap 20 w/ storage removal; the binding stamp `{workflowId, refresh:'on_run'}` lives HERE). run-workflow's merge dedupes by id (**NEVER A TWIN** — a head replaces in place; every other kind appends byte-identically). `?v=<n>` on the authed door only; the PUBLIC door is structurally query-blind (**a share is a view of the present** — old numbers are a retention surface a link must not widen). ONE shared `FrameVersionPicker`; Share renders ONLY on Current. `/frames/[id]` = THE ADDRESS (always current); from-run (`lib/frames/from-run.ts`) JOINS the series, never founds a sibling.
- **THE SHARE LINK**: `item_plans` kind `frame_share` (no migration — the workflow_owner store precedent; unique key = one live share, re-share returns the SAME token), crypto token never logged, public page `/frames/shared/[token]` OUTSIDE (main) (no-store every exit — revoke = instant 404 byte-identical to never-existed; a dead link never bounces to /login), disclosure verbatim "Anyone with this link can view this frame and its data.", THE SOVEREIGN FLOOR (`features.email === false` → spoken 403 before any write — policy speaks, secrecy doesn't). Per-link revoke is the whole control surface for now (owner call); the workspace kill-switch is designed, unbuilt.
- **Surfaces**: the Claude idiom — inline chat card → Open raises the SIDE PANEL (the shared artifact panel) → "Full screen →" the address; the deep-dive Frames tab = a 2-col gallery (REAL mini previews via the one renderer, served visibility only, live mark, "· N versions") + the from-run picker (time + deliverable title + "has a frame" chip). NO explainer card (owner removed it — the claims floor lives in gates: no revision promise, no fabricated counters; kind chips deferred until a kind is stamped at generation).
- **Deferred**: Phase 4 parameterized recompute (crosses THE HUMAN-IN-THE-LOOP LAW — own spec addendum); workspace share kill-switch; AgentOS Python task-tool enum lacks 'frame' until a box redeploy.
- ⚠️ Suite lessons: Supabase `storage.download()` reads through the CDN and can serve DELETED objects — retention/existence gates must use metadata `list()`; a raw `https://` grep is not an egress law (xmlns/SVG-metadata false-fires) — sweep with the validator's own attribute net after stripping xmlns declarations.

### THE PROCESSES ARC (Aug 18–20 — plan `docs/processes-plan.md`; suites smoke-handoffs **204/204** · smoke-processes **66/66** · smoke-run-record **102/102**; committed through `c03075a` + the frames-adjacent walk fixes)

Runs became collaborative PROCESSES (the owner's mockups): a run = a case with a subject, people, and a waiting-on. Laws: ONE DERIVATION (`lib/workflows/process-state.ts` — every surface consumes served state) · ONE DEED ONE DOOR (every approve/reject = `/api/workflows/runs/[id]/resume`) · NO SECOND ROOM (`run:<id>` loose rooms, CREATOR-keyed — comments AND decision narrations in one room; ownership transfer must never split the trail).

- **HANDOFFS (Phase B)**: `handoff` step type {assignee workspace member, ask, sla_hours} — parks with the gate belonging to the ASSIGNEE (their deck ask + room decision card + coworker email with the deep link; the park's limbs are independently best-effort); `canResumeRun` (owner always · assignee their gate · stranger indistinguishable 404); SLA chases ≤1/day (atomic insert-first cap); REASSIGN via per-run `handoff_override` in item_plans (the authored step never mutates; rev-suffixed dedupe keys). **THE SENDER FALLBACK LADDER**: a coworker-less workflow's park/nudge email speaks as the OWNER'S PERSONAL ASSISTANT, never generic team@ (one seam, gate H16). Name→member resolution (`lib/workflows/resolve-member.ts`): exact → bare-first-name → every-token containment; AMBIGUITY IS A REFUSAL; deterministic workspace = oldest membership (`joined_at` — the silent-column trap bit here).
- **B2 + METRICS**: owner = the ACCOUNTABILITY layer (item_plans kind `workflow_owner`; execution identity stays the creator — THE SPLIT LAW); the Metrics tab (authored `estimated_manual_minutes` baseline — never model-estimated; real tokens/cost from ai_usage_events in EUR; time-saved always "your estimate").
- **THE RUN RECORD + COMMENTS (mockup-fidelity)**: `lib/workflows/run-record.ts` — decisionsOf NEVER fabricates (multi-handoff runs refuse to borrow an SLA; a test auto-pass is not a human decision; a nameless handoff decider is "A teammate", never "You"); pure driftChipsOf (`Rejected · Handoff over SLA · Owner changed · Review step skipped`) + vsPrevious; `canReadRunRecord` = ONE visibility predicate (past holders READ, resume stays current-holder). Comments = role-'user' turns in the creator's run room behind ONE authorized route. History rows open the READ-ONLY record drawer (Decisions | Log | vs. previous — zero affordances). The deep-dive: process TABLE (served facepiles/waiting-on; Show completed DEFAULT-ON; the Log cell opens the drawer's Log tab), Timeline bar-end labels, the composer = the standing room's door (words persist FIRST; hidden without a binding).
- **THE BADGE POINTS AT ITS ROWS**: the nav Workflows badge (unreviewed succeeded runs, 30d) serves its per-workflow share on the ledger (same predicate, dedicated query — badge N = Σ row pills); every reviewing deed clears through the ONE `reviewed_at` stamp (open deliverable · expand trail · deep-dive mount). Ledger route flattened to ONE Promise.all flight (~7 sequential round-trips died); cold paint wears a skeleton in the row's own shape; the SSR'd header date is hydration-guarded (the client owns the clock).
- **Deferred on named triggers**: THE REPLY RAIL (reply-to-approve — trigger = SLA receipts showing asks dying at the login wall); ghost agents (specced, owner layer now exists); reference IDs; drift analytics. ⚠️ Known UX gap (owner-found, queued): the handoff commitment room must CARRY THE WORK BEING GATED (deliverable preview on the decision card; run context instead of "no linked source"; one decision surface).

### THE GUARDRAILS ARC (Aug 14–15 — plan `docs/guardrails-plan.md`; suite `scripts/smoke-guardrails.ts` **74/74** at gate v5; committed `18598ce`)

The workflow gate stopped being mute, unsteerable, and dead-ended (pilot feedback: visible per-step QA, custom rules, auditable runs). **VERIFY_GATE_VERSION 5** (`lib/workflows/execute-step.ts`):

- **THE STRUCTURED VERDICT**: the verify step returns the corrected draft + `GateVerdict {status, findings[], reported}` via a `===GATE_VERDICT===` sentinel (parse LAST occurrence; degradation = deterministic-floor findings + `reported:false`, NEVER a fabricated pass; the sentinel never leaks into a deliverable — `body || raw`). Verdict rides `StepOutput.verdict` → `workflow_runs.step_outputs` jsonb → every runs surface with zero route changes.
- **THREE LAYERS, NONE GUESSED**: built-ins (static truthful map `lib/workflows/builtin-checks.ts` — claims only what executors actually do) · **THE BRIEF** (the producing step's OWN prompt enforced AS STATED, passed as `ctx.producingPrompt` by the run loop — positional: only the step feeding the gate) · **YOUR RULES** (`VerifyStep.rules`, ONE gate, never per-step fragments) + **THE STEP'S OWN ASK** (`ToolStep.check` + `AIStep.check` — authored on the step's shield node, aggregated by `stepChecksFor` into the ONE gate at BOTH execution sites, findings attributed via `stepLabel` with a STRUCTURAL code-side fallback). Prompt ≠ check: the prompt is what to make (hoped for); a check is what must be TRUE (receipted).
- **CODE-ENFORCED BOTH DIRECTIONS**: `blocked` honored ONLY with a `source:'rule'` finding (no blocking on vibes) AND a block/hold/stop-demanding rule + rule finding FORCES `blocked` (v3–v5 lesson: prompt-only enforcement of the user's stated escalation coin-flipped across runs — the promise moved into code; **re-earn green after EVERY prompt change**, the rerun caught the regression).
- **RETRY-THEN-HOLD**: blocked → the producing step re-runs ONCE with `guardrailFeedback` (all three prompt sites incl. AgentOS) → still blocked → park through the EXISTING `awaiting_approval` machinery (`narrateGuardrailHold` = same approval room component; `/resume` unchanged — seeding lands AFTER the gate, provably never re-run). Test mode NEVER parks. Deterministic `gateNote` rides the report-back ("corrected 2 figures, masked 1 item under your rules").
- **STUDIO** (`studio-builder.tsx`): verify/approval render as pill STATIONS, not step cards; **THE PINNED STATION** — `seatGate` normalizes on every steps mutation (the gate drifted mid-pipeline on the owner's first walk, standing as a false claim; verify excluded from reorder); **THE PROTECTIVE DEFAULT** — auto-seats when the user's action adds an EXTERNAL_MATERIAL_TOOLS step or an email/slack home (never on load; removal sticks per session); built-in edge chips + AnchoredPopover with the per-step check editor; the rules editor + starter chips; Test-run verdict chips with action-verb findings; **NO LYING DOORS** — the inline + is hidden at/below the seated gate (seatGate would teleport the insert).
- **RECEIPTS ON THE LIVE SURFACE** (`workflows-ledger.tsx` + ledger route): RunAudit per-run GateChip + expandable findings; the grouped trail wears a DELTA-ONLY chip (clean pass silent — repeat successes don't speak). ⚠️ `components/workers/tabs/worker-activity-tab.tsx` is ORPHANED (no route mounts worker-profile) — receipts landed there first and were reverted; cleanup pending.
- **FIDELITY PROOF**: 74/74 on the probe incl. the live AHK Executive Briefing clone ×3 AND the pilot-account config replayed verbatim (all 12 steps incl. deep_research, 358s, in-format German briefing, no verdict on any step — legacy workflows without a verify step structurally cannot reach the new branches). generate-config emits `rules`; policies land on the gate, never in ai-step prose. Deferred (plan doc): PII regex + entity-registry name masking; workspace-level admin rules; brokered/MCP compiled-code lane (design recorded — pinning at build time, allowlisted egress, creds never in the sandbox).

### THE MACHINE + THE GROUND LAW + COVERAGE (Aug 13–14 — the current tier; plan entries AH–AQ in `docs/one-surface-plan.md`; gates smoke-compute **150/150** · promise **146/146** · one-room **85/85** · NEW permanent `scripts/smoke-ground.ts`)

The trust-under-change tier: "the machine reaches everything, every surface speaks it, and nothing served can lie about the present." Orchestrator model matured (Fable writes spec/core/judgment pieces; Opus subagents conform surgically with precise briefs; every diff reviewed; read-only diagnosis agents on live accounts BEFORE fixes).

- **THE MACHINE (spec Part "THE MACHINE" + `lib/work/machine.ts`)**: the explicit work-item lifecycle (unjudged → preparing → ready/awaiting_input/awaiting_decision → enacting → awaiting_approval → committed → settled · parked), derived AT READ TIME from existing truth — never a new table. TWO READERS, ONE LADDER: `workStateOf` (single) + `workStatesFor` (a whole deck from ~5 batched queries; 90 states ≈ 214ms) both call the pure `deriveState`; `poolRowsToArtifacts` is the shared pool mapper (forking it = deck vs deep-dive disagreement). `STATE_WORDS` = the ONE human mapping every surface speaks; deck rows fold the word into the muted second line (silent states render NOTHING; awaiting_approval defers to the prepared chip; ONE CLAIM PER ROW — ask-bearing states null the prepared chip/CTA); the deep-dive stage header speaks the same word; "surfaced today" rides first-judged-within-24h rows (the backlog drain must read as catching up, not pop-ins). `lib/room/render-plan.ts` = the placement table (one renderer, both doors; THE MOVE YIELDS to a rendered decision — the card IS the CTA). THE LADDER's found-live rules: decision material = brief OR the verdict's own options; the OPEN ASK outranks a staged send; unfireable sends (timeless invite) are NOT send-shaped; >48h judgments with nothing landed read unjudged; superseded work reads preparing.
- **THE GROUND LAW (spec section + `lib/prepare/ground.ts`)**: every prepared artifact stamps `prepared_from` (the newest inbound at prep time); a ground move supersedes STRUCTURALLY — the verdict can stay identical while content goes stale (proved by read-only diagnosis of a live room: reactivate stamps activity then stops; one post-inbound regeneration makes the last_activity guard permanently dead; the verdict-shaped stripper never catches wrong-content). Stale is DERIVED at read (`getPrepared`), never stored; unstamped = exempt. All 8 lanes stamp + re-prepare on ground move (old rows version); ONE delta narration (`narrateGroundMove`, deduped per item+inbound); the on-open trip re-prepares in after() when a stale view serves; **THE CONTENT HALF** — `generateReplyDraft` answers the thread's PRESENT (newest inbound, topMessageOf-stripped; founding message demotes to context) — caught by the suite itself re-confirming a dead plan with a fresh stamp. Narration expires with the brief (fold under "earlier"); THE EDITOR sees MACHINE STATE + NEWEST MESSAGE + the one-claim-about-what's-owed law (ROOM_BRIEF_VERSION 6); THE ALREADY-BOOKED FLOOR (an invite whose attendee already has a calendar event ±12h prepares nothing + strips the unsent duplicate); THE RESCHEDULE RE-BRIEF (anticipation fire key carries start_time).
- **THE SCENARIO MATRIX (3 Opus agents: verb-lane walk · ask-journey walk · read-only pilot census)**: B1–B5 (send_file no-match RAISES the ask; invite startISO read; nudge attribution; commitment decision grounds on its OWN row; EMAIL_RE punctuation ×3 copies) + D1–D8 supply-loop repairs (the coworker-supersedes delete gated on needsInput — awaiting_input was STRUCTURALLY UNREACHABLE for coworker-produce; evidence token-subset fallback; SUPPLY RE-OPENS THE WORK; ingest archives engine asks whole; user files ≠ prepared work; THE ASK-DIRECTION FLOOR — our ask to the user is never the counterparty's debt) + machine-ladder repairs re-censused CONFIRMED on the pilot account. **JUDGE_VERSION 17** (15 ask-direction · 16 booked-calendar fact · 17 wait-until clamp — P25 caught the dilution); **STATE_PROMPT_VERSION 8** (the one-claim law at entity-state synthesis; verdict digest rides the sig — the frozen "no reply needed yet" contradiction class).
- **THE COVERAGE REPAIR (`app/api/cron/draft-sweep`)**: the pilot's ~5% judged coverage root-caused — 18 profiles shared 240s SEQUENTIALLY with a 20s floor = 360s in a 300s route; the route died mid-loop and tail users NEVER got a pass (a 22h gap live). Now: ACTIVE users only (connections OR recent items/meetings — sovereign-safe), LEAST-RECENTLY-SERVED first (self-balancing), a wall-clock guard + `usersLeftBehind` honesty, budgets 30–120s over the real count.
- **THE FRAGMENTATION FIXES (four owner walks, one morning — every one a class)**: THE ORPHAN-PREP FOLD (a prep:* narration with no artifact NEVER stands alone, even with the brief version-invalidated); THE HISTORY DRAWER (the "earlier (N)" fold at the TOP of the room, past above present, pure record — no cards/affordances); SERVER TRUTH WINS INCLUDING DELETIONS (the hydrate merge kept local when LONGER — a deletion makes the server shorter by design); **THE NARRATION FOLLOWS ITS ARTIFACT, UNGATED** (apply-verdict's narration delete lived inside `if (changed)` — an artifact dying through another door orphaned its "ready to review" line as a standing lie; now a prep narration survives ONLY while the current verdict's lane holds its artifact). Earlier same-tier: THE TRICHOTOMY (every judged-actionable lands PREPARED/ASKED/PARKED), THE DECISION HAS ONE SURFACE, THE EDITOR (compose keep/moot ask verdicts · TEAM_VOICE), THE FORWARD-MOTION LAW (menu clicks execute contracts; ref-tag floor; settled engine asks archive whole), THE CONTAINERS LAW (coworker DM = ONE continuous thread; room = continuous record; chief chat = sessions).
- ⚠️ Lessons that bit twice: `room_turns` has NO `updated_at` column — the Supabase silent-column trap turned two deletion checks into false "already deleted" reads (surface `error`, always); exact `VERSION = N` pins in gates break on every bump — use version-log strings/floors; a version bump invalidates every cached brief and every fold rule keyed on brief-presence switches off in that window (fold rules must not depend on brief presence).

### THE DOCUMENT HANDS + THE ONE PRODUCTION DOOR (Aug 11–12 — plan entries YY–AF in `docs/one-surface-plan.md`; gates `scripts/smoke-compute.ts` **134/134** + replay suite on the probe)

The "as good as Claude on documents, plus memory/branding Claude can't do" arc (benchmarked against two real Claude sessions: EG Bank CSV→branded charted reports; STC Bahrain recalibration), closed by the convergence law.

- **THE ONE PRODUCTION DOOR (`lib/documents/materialize.ts`, plan AF — the arc's capstone)**: every document ANY actor ships — chief delegations (`runDelegation`), coworker DMs (`generateThreadDocument`; the legacy generators pipeline DELETED from both runtimes — native chat route + AgentOS call the ONE function), workflow runs — materializes through ONE module: the tier ladder (compiler → typed → branded renderers) + the deterministic floors (THE CONTENT FLOOR: the author's text IS the document's text — codegen formats, never authors, found live as perfect-structure-zero-facts; THE FACTS FLOOR: tabular material without precomputed facts gets `computeDataFacts` AT THE DOOR — model discretion never decides whether numbers are computed; THE THEME: one getDocTheme hierarchy). **THE INVARIANT: adding a document capability = one edit at the door, every actor upgrades at once.** DM revision VERSION-APPENDS (parent_id — the panel's version chain) vs the Home card's replace-in-place — each surface's idiom. Revision + tabular material AUTO-RESOLVE from the thread INSIDE generateThreadDocument so neither runtime can drift.
- **THE DOCUMENT COMPILER (DH6, `lib/compute/document-compiler.ts`)**: code-per-document in the locked room — generated Python (python-docx/pptx, openpyxl, matplotlib Agg, LibreOffice, tesseract + our `infra/compute/helpers/augmtd_docs.py`: `clone_slide` w/ rId remap, `render_verify`, `ocr_image`) behind THE RENDER-VERIFICATION GATE (LibreOffice→PDF page count printed "RENDERED PAGES: n"; soffice needs `-env:UserInstallation=file:///tmp/lo_profile` + HOME=/tmp on the read-only fs — rc=77 otherwise), TS-side zip/PDF magic validation, THE AGGREGATES-FROM-ROWS rule (found live: "overall average" shipped as unweighted mean of group means), ONE stderr-carrying repair → honest null → the template tier is the floor. ⚠️ Supabase storage CDN caches 1h by default — a revision reusing its path served the PRE-revision file; both upload doors pin `cacheControl: '0'`.
- **REVISION-IN-PLACE + TEMPLATE-BY-EXAMPLE (DH7)**: assistant turns send their document card ref back in history; the classifier judges `revises`; the prior artifact's bytes mount at `/job/inputs/current.<ext>` behind an OWNERSHIP FLOOR (client-supplied ids + admin client → thread must be THIS user's); result lands on the SAME artifact id. "Follow this template" resolves attached office bytes (≤1MB ride `ConverseAttachment.file`) or a NAMED KB doc → `/job/inputs/template.<ext>`.
- **THE DATA-BY-CODE LANE (DH5, `lib/compute/data-facts.ts`)**: tabular chat material → codegen (THE DATA PREVIEW: real header+rows) → sandbox → COMPUTED FACTS ride the delegation as AUTHORITATIVE. Floors: ROW-COUNT ("TOTAL ROWS: n" code-verified), PARTITION ("CLASSIFIED: n" == total; user-stated bands leave decimal gaps — closed half-open + disclosed Note), request coverage. Still-wrong after one repair → NULL (wrong facts worse than none). ⚠️ native `executeAgentStep` is a TOOLLESS single completion — deterministic lanes route around it.
- **Typed deliverables (DH3) + branded kit/moment theme (DH4)**: ```spreadsheet/```slides fences → ONE code-validating parser both engines share; DocTheme (logo/accent/footer) hierarchy = request override → user saved (`item_plans` kind `doc_theme`) → workspace `settings.branding` → house; "brand this with the attached logo" extracts a palette from pixels on the spot (`themeFromLogoBuffer`).
- **DM first contact (pilot feedback)**: the coworker introduces THEMSELF (first-person, their avatar via `WorkerFace` — a self-introduction is honest speech; the one-narrator law guards engine narration, not greetings) + tappable example chips; the team explainer shows exactly once (LS flag). Sidebar: Sources category dissolved (Inbox/Meetings under Workflows); Meetings "New note"→"New meeting". Aug 12 polish (iScore walk): `WorkerFace` 20→28px (owner: bigger coworker faces in chat); the sidebar facepile fetches the roster ON MOUNT when the LS cache is empty (it only fetched on popover-open — a fresh device sat grey until clicked); chat text is the app sans (THE VOICE serif retired from chat — see the one-surface section).
- ⚠️ **Anthropic API credits ran dry Aug 11** (standard-tier conversation/generation died platform-wide, incl. prod) — topped up Aug 12. The mitigation path proven: a `tenant_configs` row flips a user to `bedrock_optimised` with zero code change.

### THE SOVEREIGN DOOR + THE PILOT-PARITY REPAIRS (Aug 8–11; plan entries V–SS in `docs/one-surface-plan.md`; replay suite `scripts/smoke-converse-history.ts`)

⚠️ **THE HUMAN-IN-THE-LOOP LAW (owner, Aug 11 — outranks all autonomy ambitions in older plan docs)**: "dangerous territory to have stuff done without human approval — we should be human in the loop." NO action (send/post/book) ever fires without explicit human approval. The autonomy arc (strategic ask · ledger · autonomous sends behind floors) was built, 10/10 E2E-proven, and PARKED the same day: `lib/autonomy/{ledger,send}.ts` stay as ⚠️-headered design records referenced by NOTHING; gate AU1 enforces the park; the outcome log keeps collecting (R1, collect-only). If ever revisited: insight-only first, owner's word before wiring any action.

Two arcs on top of THE ONE SURFACE, both deployed. Plus (Aug 11, plan SS): **THE USER-VOICE LAW EXTENDED to worker threads** — recents list a coworker thread only with a REAL user message (updated_at alone paraded system use as conversation); **THE FRESH FLOOR** (hardened twice same morning) — NO implicit conversation rehydration on landing (the stored key restores ONLY behind the explicit cross-page intent flag, else it CLEARS — a "fresh" chat must never silently append to an unseen old room), and sidebar Home resets COMPLETELY (DM mode/turns/scope/key — the deck composer can never say "Message Clara…"); the deck + an empty chief chat are the default; past conversations live behind explicit doors only; **THE DM HISTORY** (`/api/workers/dm-sessions` + the header popover beside New session, first-ask titles).

- **THE PILOT-PARITY REPAIRS (Aug 10 — René ran the same tasks in Claude and us, side by side; every find was a CLASS)**: (1) **THE PANEL TRANSCRIPT** — the Home chat's history reaches EVERY converse path (router · agent loop as REAL messages · delegation), not just the question path ("yes please" had arrived with nothing to say yes to). (2) **THE PRODUCTION HAND-OFF** — the paste ceiling died (ask door 500→20k chars; steer matches; route budget 180s); produced work without a named coworker DELEGATES to the fit from the classifier (Sofia default); loop exhaustion hands the work + full material to Sofia automatically (the bare "I couldn't finish that one." is dead); delegated deliverables keep every section and mark user-only facts `[CONFIRM: …]`. (3) **THE ATTACHED MATERIAL** — `/api/home/extract-attach` extracts synchronously; text rides the ask itself (classifier sees names, loop carries it as its own turn, delegation carries it whole) — never a race against KB background indexing. (4) **TOKEN STREAMING** on the agent-loop path (SSE `token` events, NUL-sentinel preview reset, 15s ping keeps 90s hand-offs alive; the question path stays non-streamed by design — JSON contract). (5) ⚠️ **THE FORMAT-FLOOR FIX**: Anthropic's OpenAI-compat endpoint began rejecting `response_format:{type:'json_object'}` (400 "Input should be 'json_schema'") — `aiCreate` STRIPS it for anthropic.com baseURLs (one transport-layer fix for all 13 call sites; parsers already handle fenced JSON). (6) **DRAG-AND-DROP ATTACH**: the WHOLE WINDOW is the drop zone while a composer mounts (a missed drop used to navigate the tab away); accepted types = everything the extractor reads (pptx/xlsx/csv/doc were rejected by ALLOWLIST DRIFT below the extractor's real ability); browser mimes are unreliable for dragged Office files — extension fallback at chat-attach + presign + the chief client; rejected files toast. **The honesty-floor misfire gate**: the registry pointer fires only when the DENIAL SENTENCE itself names a registry entity. **QA lesson institutionalized**: pilots break us on TURN 2+, with real files, against silent caps/allowlist drift — the replay suite (T2–T7) is the seed of a standing pilot-parity practice.
- **THE SOVEREIGN DOOR (Aug 10–11 — the corporate tier; enterprise clients who refuse Google/Microsoft OAuth, the iScore blocker class; gates SV1–SV6)**: **`features.email === false` is THE trigger.** (1) **THE LEAK AUDIT**: the Home first look pivots to "Set up your agent team" (the connect-inbox CTA never consulted the flag); Settings hides the Email tab AND bounces direct ?tab=email; the chief's toolset drops mailbox verbs (send_prepared_reply/prepare_forward registered in TOOL_FEATURE; converse's agentLoop filters its defs by workspace features). THE BOUNDARY: auth connections only — workflow email SENDING (Resend, stated addresses) stays. (2) **THE ENTERPRISE DOOR (Aug 11 — generalized from the branded entry)**: ONE generic corporate entry at `app.augmtd.ai/enterprise` (`app/enterprise/page.tsx` + `components/auth/enterprise-entry.tsx`) — the workspace CODE identifies the company, so the door carries no per-client branding (the sidebar co-brand keeps the client's mark once inside); the old branded `app/[slug]` catch-all survives as a REDIRECT (real active slug → /enterprise, anything else → /login — never an enumeration oracle). Same split screen as the normal onboarding (`RightPanel` EXPORTED from onboarding-client, never copied); email+password only. BOTH walks live on the one door: create account (ONE screen — full name · work email · password · workspace code, passive terms line; confirm-email returns via `auth/callback?next=/enterprise`, relative-only) AND Sign in (signInWithPassword → full navigation back to /enterprise so the SERVER routes it: active member → /home, authed non-member → the code step) with **Forgot password** (resetPasswordForEmail → the emailed link lands on `app/enterprise/reset-password` through the same callback; the sent-screen never confirms account existence; an expired link gets an honest dead-end — the corporate tier is the ONLY password-auth surface, so this page is the whole reset story). **THE INTRO STEP** (post-join, one screen, skippable): role · "what does your work mostly involve?" — the NAME rides in from signup (re-asked only on the confirm-email return mount; `POST /api/user/intro` falls back to the signup metadata) → `profiles.full_name` + `ProfileLoader.initializeUser` + identity `responsibilities` — lands in the SAME `context_profiles` identity row `buildUserContextBlock` renders as ABOUT YOU in every prompt (a sovereign account has no mailbox to learn identity from, so this is often the only day-one identity signal). **Sign-out is workspace-aware**: `auth/signout` resolves the workspace BEFORE killing the session — `features.email === false` → /enterprise, else /login (best-effort, /login fallback). Platform-admin's workspace-detail Entry link copies `/enterprise`. **THE TEAM NEEDS NO ASSEMBLY (Aug 12)**: coworkers are seeded at join, so the sovereign empty Home shows THE TEAM PRESENT — `components/home/team-ready-card.tsx` (four faces + names + role labels, each a DM door; "Show me around" reopens the wizard) replaced the "Set up your agent team" framing — plus **THE WELCOME WIZARD** (`components/home/welcome-wizard.tsx`: meet-your-team · create-workflows · record-and-organise; auto-opens ONCE per account via auth-metadata `welcome_wizard_seen_at`, no migration; every exit path stamps it). **THE SOVEREIGN CENTERPIECE + THE INTAKE + THE USER-CONTEXT LANE (Aug 14)**: on an email-off workspace with an EMPTY deck the conversation is the front door — the team card + composer center as ONE group (`sovereignCenter` in home-view: class/spacer toggles on the SAME mounts, never a remount; the sticky floor returns when chat goes live or rows exist), with day-one chips that claim only what a no-mailbox account can deliver (standalone drafts · attach-a-file · web research — "Plan my week" with no calendar is hollow; a chip is a CLAIM). **Proactivity comes from the COWORKER, in their own voice (owner correction — never a chrome label on the general Home)**: Clara's DM first contact on sovereign workspaces is the intake — intro bubble PLACING the whole team (live roster names + seeded-name specialties, custom workers degrade to names; independent of the one-time explainer LS flag) then the QUESTION in its own bubble ("what's your role, and what's the main thing on your plate this week? Anything you tell me here, we all remember") + context-free chips; one greeter, not four; consecutive same-author bubbles share ONE face+name header (the Slack grouping rule, in the panel render). **THE USER-CONTEXT LANE** (`lib/context/intake-memory.ts`): facts the user STATES about themself land in USER-LEVEL memory — merge-only into the `context_profiles` identity row (role fill-if-empty · responsibilities · free-form `notes`, now rendered as a `Context:` line in `buildUserContextBlock`'s ABOUT YOU) — never only the interviewing coworker's private memory; **THE POVERTY GATE** (email-off OR zero connections, checked before any AI spend) means warm mailbox accounts are never interviewed and never billed; wired at TWO seams only — inside `extractAgentMemory` (both DM runtimes ride it) and an `after()` on `/api/home/ask` — one classification-tier pass, stated-facts-only, `NOTHING` sentinel, failure never touches a conversation. QUEUED as slice 2 (owner-agreed): the facepile UNREAD PING — pure unread semantics (coworker messages since last thread open, server-side read marker), a conversation affordance on the facepile, NEVER a second attention queue (the deck stays the only attention surface). **THE SOVEREIGN COPY LAW extended to static strings**: email-off workspaces never read mailbox framing — portfolio empty state, New-project modal subtitle, add-item-picker placeholder are feature-aware via `useFeatures()`; and the portfolio's `hasMemory` early-return no longer hides "New project" (found live on iScore: a sovereign account has no mail to bootstrap memory from, so the old gate was a PERMANENT dead end — creating a project must never wait on the memory). (3) **THE SAFE-DATA MARK**: "Private environment" on the door + the sidebar footer; client logo co-brands the sidebar wordmark (`companies.settings.branding`). (4) **PLATFORM-ADMIN REDESIGNED**: the list is a READ-ONLY INDEX (name gets the room; quiet badges; whole row opens the detail page); **`/platform-admin/workspaces/[id]`** holds ALL editing — identity, the Corporate toggle, entry link, **branded join codes** (editable, ISCORE26-style, format+uniqueness server-side), branding (logo UPLOAD → public `branding` bucket, raster-only ≤2MB), features in current names (Drive→Knowledge, Studio→Workflows), members (role change + FULL user delete, same routes), danger zone. Superadmin = the dedicated `superadmin@augmtd.ai` account (not automatable — owner eyeballs admin surfaces). (5) **THE TEAM ARRIVES WITH THE MEMBERSHIP (found live day 1: the first iScore user had ZERO coworkers)** — worker seeding was coupled to the EMAIL bootstrap; now `/api/company/join` seeds in after() AND the presence route self-heals an empty roster on any visit; the workflow template gallery hides mailbox-READING templates on email-off workspaces (generate-config already excluded mailbox tools by feature). (6) **/work + /workers RETIRED** (redirects preserve deep links → the Home conversation opener); **the meeting assistant (auto-join bot) UI retired** (settings card deleted, Send-assistant gone, admin toggles gone; bot routes + Hetzner infra DORMANT — the same service runs in-person recording, which stays; `bot_manager` insight generation serves the recording pipeline).

### THE ONE SURFACE (Aug 5–6 — plan `docs/one-surface-plan.md` = THE ROADMAP; gates `scripts/smoke-compute.ts`)

The strategic arc after the agent-workspace review (Cloudflare OS et al): **the agent loop is commoditizing; the brain is not** — conform on the chrome (the Claude/ChatGPT conversation idiom), differentiate on the brain. Plan doc carries: the work LADDER (throwaway ask → loose conversation → loose item → project item → explicit → standing; origin never determines treatment — recognition+judgment do; **sidebar lists CONVERSATIONS, deck lists ATTENTION; chat is cheap, objects are deliberate; HISTORY IS THE DEFAULT, temporary is a choice**), the seat-continuity clause (Arc 3 reassigns NO experience-spec seat), the thread-management story (sidebar recents · All-conversations view · interim History picker), and the execution roadmap ("fresh chrome, same organs": shell → smoothness → absorption → tail).

- **ARC 1 — COMPUTE (the deliverable ceiling)**: the sandbox (`infra/compute/` on the Hetzner box :8002, DEPLOYED — --network none job containers, declared read-only inputs, hard caps, non-root; the locked-room network-refusal proof is a live gate) + `run_compute` (one registry row → chat/workflows/Studio picker; outputs → work-artifacts + KB + entity_id stamp). **THE ARITHMETIC FLOOR** (`lib/prepare/verify-claims.ts`, mounted in the evaluator): computable claims (sums/%-changes/date↔weekday EN/PT/DE/FR) recomputed BY CODE; THE QUOTE LAW + THE OPERAND LAW (an extractor-invented operand drops the check — found live) + failure≠verdict; asymmetric by design (a missed catch possible, a false revise structurally not). **PRODUCE COMPUTES BEFORE IT WRITES** (`lib/prepare/compute-produce.ts` in the pass's produce lane): staged kb data files → codegen (THE CLOCK — an assumed-year filter printed a confident 0, the year-transposition class reached codegen; THE DATA PREVIEW — a guessed column KeyError'd; REASONED REPAIR — the repair prompt carries WHY) → sandbox → COMPUTED FACTS ride the envelope; **THE PROVENANCE CHIP** ("✓ computed in code") renders ONLY from the structural `provenance.computed` stamp. Env: COMPUTE_SERVICE_URL/COMPUTE_SECRET (Vercel + .env.local).
- **ARC 2 — EXECUTION CONVERGENCE (workflows = standing commitments), COMPLETE**: **THE STANDING BINDING** (`lib/workflows/standing.ts`): a scheduled workflow holds exactly ONE open commitment (source='workflow', due_date = next run), advanced on success, re-synced hourly by the dispatch cron (self-healing) — a silently-dead task stands as an OVERDUE DEBT on the deck; a human dismissal STICKS; **THE JUDGE FLOOR** (source=workflow → structural none — its workflow produces it); **THE MISSED-PROMISE FLOOR** (the dispatcher pre-advances next_run_at, so only `fromSuccessfulRun` may advance a PAST due date — a failing-every-run task stays visibly owed). **THE SPEC CARD** (`lib/work/standing-spec.ts` + `/api/tasks/standing` + the rail card): "weekly report on X" said anywhere → code-validated spec (cron proved by nextRunFromTrigger; real coworker owner) → durable `standing_spec` component turn in the work's room → the card's ONE Confirm (exactly-once) → executeCreateTask + the binding; SAYING PREPARES, COMMITTING STAYS EXPLICIT. Runs NARRATE into the standing commitment's room (`narrateStandingRun`; failure stamps due_date today); room feedback mutates the method (`steer_standing_task` → dated STANDING FEEDBACK appended to worker_instructions, tail-capped); Studio demoted to the method editor (the card's quiet "method" link).
- **ARC 3 — THE SHELL (in progress)**: Drive OUT of the nav (Settings → Knowledge is the door; route survives); Settings → Team (the fold's config door); **THE DURABLE HOME CHAT** (the Home conversation = a `chat:<uuid>` loose room — persists, rehydrates, "New" starts fresh; **THE HISTORY PICKER lives INSIDE the chat panel**, chat rooms titled by their own first ask; 62vh takeover; ⚠️ **THE VOICE serif RETIRED from chat (owner, Aug 12: "keep it consistent with the rest — any AI chat")** — `.font-voice` stripped from home-ask answers + item-rail room briefs; chat text is the app sans; only briefing-view still carries the serif, pending the same call). **The Aug 6 lesson (owner-corrected twice, now law): an end-state mockup element gets NO interim approximation** — a dimension earns its seat when its arc lands (two interim conversation surfaces were built and killed the same morning). `/api/rooms/recent` stands ready for the fold. NEXT: the fresh shell (the fold wholesale), streaming ask, the absorption + composer consolidation, Knowledge panel, frames.
- **Conversation capabilities added**: `read_action_history` (the ledger read over activity_events+action_commits+email_sends, boundary-declared), `propose_standing_task`, `steer_standing_task`, **the one-grounding focus** (`findEntityFocus` in lib/home/ask.ts — a Home question NAMING an entity gets that entity's FULL room grounding appended; strict distinctive-token match, all-generic names never match; grounding tags stripped) — "status on X?" from the Home and from X's room are the same answer BY CONSTRUCTION.

### THE PROACTIVE TEAM (July 28–30; plan `docs/proactive-team-plan.md`, memory `project_proactivity_audit.md`)

The judge got hands, honesty, a clock, and a client map; the room got one voice. Six workstreams + trust fixes, all gated (smoke-promise **P1–P32, 138 gates**, outcome-asserted on real accounts + the probe host). Migration `20260728_action_commits.sql` APPLIED.

- **THE ONE SYSTEM (Aug 5 — "it should feel like Claude": one grounding, one responder, surfaces as views; five parallel panel authors died)**: (1) **THE ONE GROUNDING** (`lib/room/grounding.ts` `assembleRoomGrounding`) — everything the brain knows about ONE room on one page: entity state+goals/rules, ledger ([L#] refs), files ([F#]), **THE LIVE BOARD** (each linked item's judged verb + its ACTUAL prepared state read from the same source_data the cards render — the merge no consumer held, the root of every "drafted" vs "nothing's prepared" contradiction), open asks, transcript. Consumed by the responder, `answerEntityQuestion` (rewritten over it), and the converse agent loop — an answer structurally cannot disagree with the panel. Adding a knowledge source = one section here, visible to ALL reasoning at once. (2) **THE ONE RESPONDER** (`lib/room/brief.ts` v3): ONE reasoned pass emits the whole opening `{brief, MOVE, offers}` — the MOVE's target is model-picked but CODE-VALIDATED against the board refs (an invented deed renders nothing, never a dead link); offers are UTTERANCES (chips send their `say` through the one composer — clicks are words, literally); the sig carries the **board digest** so any preparedness change recomposes. Served on `RoomEntity.brief/move/offers` + the loose view. (3) **PARALLEL AUTHORS DEAD**: entity-room's `openHref` only FOCUSES (click-echo narrations + "want me on it?" offer pushes deleted); the rail's "Next:" line survives only as pre-compose fallback; the hand-off pill arrives as a responder offer. Left panel = brief → MOVE card → conversation → composer (the Superhuman/Linear/Fin pattern: one state, one recommended action, one affordance grammar). Gates R13 incl. live board-validation (one-room 85/85; promise 146/146). Also Aug 4 evening (same tree): **A COMPONENT IS A TURN** (artifact cards seat at their anchor turn's chronological moment via `anchorKey` = the prep:* dedupe key; unanchored append at the stream's end — an invite never trails questions asked after it; ephemeral scaffolding via `pushDealTurn({ephemeral})`/`dropDealTurn`); **the strip 2nd pass** (Dismiss folded into a worded "More"+icon-chevron menu, divider below; stage chips removed — directions live ONLY in the exchange); **THE PEOPLE TYPEAHEAD** (`/api/people/suggest` graph-ranked + mail top-up, robots filtered; one `PeopleSuggestInput` in attendee+recipient chip fields); **THE ATTACHABLE-REQUIRES LAW** (found live: "attach a confirmation of the Thursday time" — an ANSWER classified as an artifact; JUDGE_VERSION 14 rule + `attachableOnly` reasoned floor at the ONE resolver (memoized, keep-all-on-failure) + word-boundary labels); **THE HONESTY FLOOR AT THE ANSWER DOOR** (P30 exposed it: registryMatches guarded only the search tools — a direct no-tool denial bypassed it; now no denial leaves the agent loop without a registry check; gate deny-regex hardened).
- **THE CONVERSATIONAL ROOM (Aug 4 — the room talks like a person; clicks are utterances)**: (1) **THE EXCERPT-HONESTY LAW** (`lib/utils/clip-for-prompt.ts` — found live: a normal email judged "cut off mid-sentence" because prompts hard-cut quoted text at char counts): every prompt-bound clip ends at a boundary and DECLARES itself (`EXCERPT_MARK`); every consuming prompt carries `EXCERPT_RULE` (a marker is OUR cut, never source truncation). Applied at judge (body+threadNow), state gists ×2, fulfillment, reactivate-closure, reply-directions; **JUDGE_VERSION 13 · STATE_PROMPT_VERSION 7 · FULFILLMENT_LAW_VERSION 3**. **⚠️ RE-MANIFESTED Aug 17 (the pilot delegation incident): lib/converse was built AFTER the law and hard-cut transcript lines mid-word — a delegated coworker read OUR cut as "the task description got cut off", CONFABULATED the quote, and reported itself blocked while handing back finished work. Swept: panel/room transcript lines, the router transcript, the delegation's transcript/material blocks, the viewing-item body, the exhaustion hand-off's task label; EXCERPT_RULE rides the transcript HEADERS (a tail-clip can't strip it) + the delegation contract, which also gained THE REPORT SPEAKS THE DELIVERABLE (blocked-while-finished outlawed). THE META-LESSON: the law decayed because it was a SITE LIST, not a structure — permanent floor `scripts/smoke-excerpt-law.ts` (source-level, zero AI; caught a second live seam on its first run). A law is only alive while a gate enforces it.** ⚠️ Until deployed the PROD cron re-poisons state every 2h (the standing lesson — local repairs don't stick). (2) **THE PARITY LAW** — every UI verb is sayable: converse gains `send_prepared_reply` (returns a `commit` the CLIENT fires through the ONE send route; deterministic `EXPLICIT_SEND` floor on the user's own words — a model mis-map can never mail) + `prepare_forward` (openStage signal; approve stays the commit); PLAN_VERSION 4. (3) **THE VERB-SCOPE LAW / VERB STRIP** (census-grounded: emails ~4.5k · meeting action items 81 · commitments 386 · meetings 36): verbs render ONLY with their object — one icon+word strip ON the stage attached to the item (identical loose/embedded; `objectKind` from `items/view.itemSource`); a meeting-extracted action item structurally CANNOT show Reply (Done/Dismiss instead); the rail carries ZERO verb chrome (left = brief → conversation → artifact cards at the stream's NOW edge → composer). (4) **THE REPLY EXCHANGE** (reference impl of the exchange grammar): Reply opens a DIALOGUE — offer turn + grounded direction chips (`/api/items/reply-directions`, cached; open option always last) → the pick lands as the USER'S turn → "Got it — drafting." (EPHEMERAL scaffolding — `pushDealTurn({ephemeral})` never persists; the pick+result are the durable story) → steer redrafts → the card at the stream edge. (5) **ONE NAVIGATION** — in-room rail links route through the room's opener (`onOpenHref`; entity-room passes focusFromHref/openHref); **ONE STAGE AT A TIME** (opening any summoned stage lowers the others — the covered-Open dead-click, found live). (6) **THE PROPOSE TIER on invites** (`prepareCalendarInvite`): user's tz (was UTC) + stated weekday; a stated DAY/window without a clock time earns a grounded PROPOSED time inside every stated constraint (`proposed` flag, card says so); no day stated → the card asks plainly + the artifact label says "needs a time from you" (`inviteHasTime`). (7) **ROOM COHERENCE**: the project room renders prepared work in the CARD grammar (board-derived artifacts), focus narrations NAME their subject (two adjacent lines read as contradiction), resolutions narrate ("Dismissed — undo lives in Activity"), embedded items keep their cards. Gates R6–R10 (one-room 78/78); resolution narrations + spokenName + silent auto-attach ride along. `scripts/sweep-click-echoes.ts`.
- **THE ROOM CONVERGENCE (Aug 3 — one room grammar for loose items AND projects; "a loose room is a project room with less to file")**: (1) **THE ROOM-DOOR LAW** — a project-member deck row opens its PROJECT ROOM (`projectByAtom` served beside the row tag from the ONE derivation point; `door()` on all three lanes; loose items keep `/item/<id>`). Root cause of every dead "Open project" click: `/?view=…` hrefs hit the middleware root redirect which DROPPED the query — 7 door sites repointed to `/home?view=…`, middleware preserves search, home-view + portfolio-view react to `useSearchParams` (a mount-only URL read never sees a same-path query nav — the class, not the click). (2) **THE CTA IS THE DEED** (laws 5+8): the rail's next-move click only navigates — the "Opening the next move" click-echo turn is gone (writer removed; `scripts/sweep-click-echoes.ts` drained remnants); the rail's room TITLE is the project door; the "Open project" chip died; a self-targeting link renders as plain text. (3) **THE ONE-VOICE BRIEF** (`lib/room/brief.ts`): the room's opening is AUTHORED as one colleague paragraph over judged state — never assembled from fields (the stitched summary+debt+Next lines spoke one fact three times). Sig-gated compose in `after()` on every room-door GET (entity sig · asks · prepared · latest narration · day); last-good serve; `ROOM_BRIEF_VERSION` in the cache (`item_plans` kind `room_brief`); AI-failure never blanks; v2 = the LOOSE door (`<kind>:<id>` key, anchor+turns); the rail prefers the composed paragraph on EVERY door. (4) **THE SUMMONED STAGE**: the reply/follow-up composer is an overlay raised over the truth pane by the user (artifact Open→ / the CTA row's Reply) — never docked, never auto-raised (all verdict/relevance auto-open seeds removed); the item verbs (Reply · Dismiss ▾ · Forward) live in the RAIL's one CTA row (law 7 — the right pane asks nothing; `embedded` keeps the in-stage palette). Riders: `spokenName` (an org is never first-worded — "M Condomínios Lda"→"M" was live), silent background auto-attach (no red "Could not load X.pdf" for a failure the user never caused), the signature line-break rule in draft-reply (HTML-stripped voice exemplars lose \n — never reproduce run-together artifacts). Gates R6+R7 (one-room 64/64). Queued: recipient-sent staging rule (never stage material the recipient themselves sent); commitment/meeting stage overlays.
- **THE ROOM ARC (Aug 2 — the experience spec applied to the room, one coherent build)**: (1) **ASK SUPERSESSION** — `settleAsksForItem` (lib/room/turns.ts) at all seven resolution doors: the ask's checklist strips on resolution, text stays as history (the ingest mechanic generalized); **ONE ARTIFACT = ONE ASK** — posting-side covers-merge in requirements.ts (distinctive-token match via namesOverlap); a merged ask survives until its LAST covered item resolves; repair `scripts/sweep-moot-asks.ts` (settled 7 across users). (2) **THE LIVING BRIEF** (item-rail): entity rooms open with position+debts (never mute); the ENGINE's live ask lifts into the brief with the one CTA row (coworker asks stay in-stream — they're speech); history folds past 3 turns ("earlier (N)"); DEAD-ASK remnants fold always (law 6); refs = quiet inline links, never pills; brief dedup (the debt line silent when it echoes the next move); ONE type scale (12px events, one grey). (3) **RIGHT PANE = FILED TRUTH**: next-move/you-owe/hand-off moved LEFT (the rail's next-move is clickable + narrates — the CTA-focus law relocated, `nextMoveHref` on RoomEntity); inventory behind ONE TabBar (Tasks·Schedule·Meetings·Conversations·Files·Activity); intent = one full-width Goals|Rules band (the emptied two-column grid died). (4) **LAW-6 LEDGER GRAMMAR** (found live: a delivered report re-asserted as owed by a state synthesized WITH correct facts): settled commitment lines speak history ("DONE — delivered: … (was due X)"), never open-debt grammar ("you owe (done): … (due <today>)"); + the explicit prompt rule; **STATE_PROMPT_VERSION 6**. (5) **NO SILENT CAPS on the deck pool** (an overdue obligation ranked 61st of a 60-recency-cap and vanished — a quiet thread is not a settled one): limit 250 + loud saturation warning. Gates P34 · R5b · H10 + five re-pointed (R5/R3c/R3d/5A.5/5A.6 → the spec'd design).
- **THE FRESHNESS ARC (Aug 2 — "the brain is event-driven at every layer except the middle": capture and synthesis were current, the inputs between them were frozen snapshots)**: four laws. **THE TOP MESSAGE** (`lib/inbox/top-message.ts` `topMessageOf` — structural reply-convention parsing, EN/PT/DE/FR attributions + Outlook blocks + >-runs; every judge asking "what did THIS message do" sees only the sender's own words — a delivery quoting last week's promise was judged as the promise). **TRUE FACTS OR NO FACTS**: attachment META stamps on EVERY stored message incl. the user's own SENT mail (sync); an unverifiable count is passed UNKNOWN to the fulfillment judge, never a confident zero (a false "0 attachments" blocked an honest close). **THE WATERMARK LAW**: the judge's prompt carries "WHERE THE THREAD STANDS NOW" (newest ≤2 messages, quote-stripped — a thank-you-last thread judges `answered`, never the founding ask) and every entity-ledger inbox line carries a "NOW (who spoke last)" clause (`nowByThread` in state.ts — being ledger text, it moves the sig → auto re-synthesis). **NOMINATE→JUDGE at the inbound door** (`reactivate-on-reply`): a pure closure ("Thank you!") settles with an auditable `closure_note`, never resurrects the item; failure → reopen (showing costs less than hiding). Commitments-sweep gained the W2 HONEST BUDGET (recency-first, 95s budget, `leftBehind` counted+returned). `FULFILLMENT_LAW_VERSION` rides the fulfillment cache sig (a cached old-law verdict must never satisfy the current law — the prompt-version lesson, learned a third time). Verified LIVE across users: STC + Fidelidade deliveries closed off the real sent mail, AHK settled off the thank-you, EG Bank stayed honestly owed; gates P33 (promise 143/143).
- **THE FULFILLMENT LAW (July 30, found live — "I'll share the report before Sunday" marked the deliver-the-report commitment FULFILLED; the week's top deliverable vanished from the Home)**: a message and a deliverable are different things. The structural resolvers (`resolve-on-reply`, the commitments sweep) only NOMINATE a candidate; `lib/commitments/fulfillment.ts` `judgeCommitmentFulfillment` (ONE cheap reasoned pass over the message's OWN words + the attachment-count fact, both directions) decides `delivered|promised|unclear` — only `delivered` closes; a re-promise with a stated NEW date RE-ANCHORS `due_date` (code-verified via `dateStatedInText`, future-only, `commitment_reanchored` activity); `unclear`/AI-failure NEVER closes (failure ≠ fulfillment); verdicts cache per candidate message (`item_plans` kind `fulfillment`, sig=emailId — a 2h sweep never re-burns). Repair `scripts/sweep-false-fulfillment.ts` (guarded; 19 real false closes reopened across users — partial deliveries, delegated-not-sent, conditional links) + `scripts/sweep-expired-rejudge.ts` (guarded; after ANY expiry-law change: restore→drop stale verdict→re-judge fresh — revived a 'later today' misread live). **THE STATED-DATE CHECK** (P27 hardening): a past `expired_on` also verifies in-text now (`dateStatedInText` — ISO/prose/D-M/weekday renderings; a hallucinated yesterday no longer defeats same-day protection).
- **THE PINNING LAW (July 29, found live — user-created projects auto-archived)**: a TRACKED entity is a human decision that outranks the machine — NEVER auto-archived, at ANY door (state self-heal · reconcile zero-member · registry judge · orphan sweep — all four guarded; only machine-founded untracked containers remain machine-archivable). A user's empty new project awaiting work is not a ghost.
- **THE OVERLAY LAW (July 29)**: an anchored panel must ESCAPE its clipping ancestors — z-index never wins across stacking contexts (deck rows sit in overflow-hidden collapse wrappers + transform-animated sections). ONE portal primitive `components/ui/anchored-popover.tsx` (fixed-position from anchor rect, reposition on scroll/resize, outside-click + Escape — never mouse-leave) owns all four picker doors (work-row · add-to-work-control · meeting-project-control · add-item-picker).
- **THE STAMPED CACHE (July 30)**: `saveLS` stamps `__at`; `loadLS(key,{maxAgeMs})` rejects stale (a legacy unstamped blob never satisfies a freshness demand). An ACTION surface never paints from a cache too old to trust — the deck + horizon demand 15-min freshness (an already-delivered item was flashing then retracting on every Home load). Ambient surfaces stay ageless.
- **THE ROW TAG (July 30)**: EVERY deck lane (reply · notice · commitment) derives its project tag from the item's ENTITY LINK against the tracked registry, ON THE SERVED PAYLOAD the client builds the deck from (`tagByAtom` in the brief route's return — NOT the server-side agenda, which only feeds the briefing composer; that was the first, wrong seam). Tracked-only (P15), independent of state synthesis (a freshly-filled project has null state until the pass runs). Membership is visible IMMEDIATELY: `/api/items/entity` PATCH soft-busts the brief, the Home listens for `aug:membership-changed` → background refetch, and the row wears an optimistic tracked-only tag on attach (server truth replaces it). Gates H8/H8b (work-surface 49/49).
- **ONE PICKER GRAMMAR + the horizon card (July 31–Aug 1)**: every "Add to project" door (Home deck `RowProjectPicker` · deep-dive `add-to-work-control` · meeting page `meeting-project-control` · meetings-list ⋯ menu) speaks ONE grammar — search + "Start a new project…" on top, YOUR (tracked) projects lead name-sorted (stable — no cache→fetch reorder flicker), the recognized-but-untracked tail below a **"Suggested"** divider. The meetings ⋯ menu creates+files in one motion (`handleCreateProject` returns the id; `createProject` exposed via the meetings-data context). The deck picker is INSTANT: a module-level session memo warmed by the row's existing hover-prefetch (`warmProjectPicker`), LS second, fetch = background refresh only. Deck groups fold past 8 rows behind the shared `ExpandableRows` idiom (toggle seated as a padded in-card row via `toggleClass`; hover/pin grammar untouched — a hold experiment was tried and REVERTED by owner call). The This-week rail splits at the CALENDAR week boundary (Sunday midnight — a Friday never files Monday under "This week") and each row opens `/meetings/<calendarEventId>` (the specific meeting page, never the bare list). Gates H9 + J24 re-pointed to the one adoption door.
- **Portfolio truth (July 29–30)**: the "quieter projects" fold is momentum-aware + count-gated (needs-you/overdue never fold; ≤5-row tails just show — `demandsAttention` in portfolio-view); momentum `unknown` = neutral grey "No signal yet" (an unjudged entity never renders healthy green — route default + every display fallback); the state speaks in WORDS in the row subline when it deviates (the dot reinforces, never carries alone). Deck calm groups: hover-preview + click-pin (briefly click-only, owner-reinstated July 30). LAST-GOOD on the shared `aug-portfolio-v1` cache (only a valid response replaces state; "Nothing here" claimed only after a real fetch confirms).

- **W1 — ONE REGISTRY, EVERY VERB HAS HANDS**: `CAPABILITY_MAP` (+ PLAN_VERSION + all helpers) moved INTO `lib/work/surface-registry.ts` (`lib/home/capability-map.ts` = re-export shim); each component row carries its `capability` binding; `WORK_VERBS` is the one verb list; `registryParity()` = the P21 law (a verb without a preparation path is a build error). New verb **`forward`**; `prepareOneItem` dispatches the full space: `schedule`→prepared invite (`source_data.prepared_invite`, grounded via `prepareCalendarInvite`), `forward`→prepared forward (literal-email-only recipients, body never duplicated), `produce` w/o coworker→the drafting assistant (never a silent none). `prepareAction` serves the AMBIENT stored artifact first; the deep-dive mounts Review-invite/forward from the VERDICT.
- **W2 — THE HONEST SWEEP**: the legacy draft-sweep rule loop is DELETED (auto_draft master moved into the pass, silencing only the reply lane); `runPreparationPass` walks three lanes through ONE `prepareOneItem` call site in ENTITY-PRIORITY order under a per-user time budget (route sizes it; `leftBehind` counted+logged, never silent). The pass's batch `routeTasks` is retired (judgeWork carries the executor half). **FAILURE HONESTY**: `WorkVerdict.failed` — an AI outage is marked, NEVER cached, never resolves/strips/prepares (apply-verdict guard); resolver failure emits a nothing-is-staged truth; evaluator outage FLAGS (never a silent pass).
- **W3 — ASKS WITH A LIFE**: `component.state.proceeded` on checklist turns; `/api/room/asks` = the GLOBAL ASK LEDGER (GET live asks across rooms; POST proceed = stamp + visible go-ahead turn + re-run the engine in after()); Home ambient bar leads with **"Needs your input"** (`components/home/waiting-on-you.tsx`); go-ahead works on ENGINE asks too; `resolveRequirements` honors proceeded (never re-posts); the delegation envelope carries "do NOT ask again"; the judge sees open-ask age as a fact. Archived asks never suppress (live-only, both directions).
- **W4 — DELIBERATE TIME**: `WorkVerdict.revisit {after, reason}` — none-only, structurally future-date; deck demotes via existing judgedNoneIds; **PARKED SERVE** re-serves without AI until the date (non-day sig match); on-date the prior anchor forces a fresh live re-judgment; apply-verdict NARRATES the set-aside (never resolves). Meeting-prep briefs narrate into the deal room. Attachments-as-candidates DEFERRED (P3 conflict — the obligation lives in the email).
- **W5 — THE COMMIT DOOR**: `lib/work/commit-door.ts` (claim→fire→record; failure releases; duplicate returns prior result — exactly-once at the send edge) + `action_commits` ledger, wired into `/api/items/execute` for invite+forward; ambient artifacts stamp `sent_at`. **R1 — THE OUTCOME LOG**: `lib/prepare/outcome.ts` (`logPreparedOutcome` + `estimateEditShare`) at three doors — send-reply (accepted/edited+share), execute (vs the prepared artifact), the ONE inbox resolver (unsent prepared work → discarded). Collect-only; the future learning arc + autonomy ladder feed on it.
- **W6 — TRUST & QA (the wrong-attach class, found live)**: **THE STAGING LAW** (`lib/prepare/requirements.ts`): provenance gates candidacy (auto-stage = the item's own pool material or a same-entity file; a global-KB/drive hit on a loose item → a NAMED suggestion in the ask, never staged); evidence is QUOTED and CODE-CHECKED (the quote must exist in the candidate's own text); ONE FILE ONE LABEL; 0.7 bar; `pickArtifacts` = the exported testable decision layer; `verifyArtifactMatch` = the one verifier at both doc-send doors (cross-entity rejected structurally); suggestions pass a plausibility read or stay silent. Chip dedup in `getPrepared`; MECHANICAL truncation floor in the evaluator (a mid-sentence cutoff revises, never reaches the user). **THE SENDER FLOOR** (JUDGE v11): an automated sender can never judge reply/chase (patterns += `payments`/"was unsuccessful", BOTH copies — automated.ts + notice-demotion.ts); the deck's judgedNoneIds demotion respects the notice law (you_owe action outranks judged-none). **QA doctrine locked**: every trust bug becomes a permanent DECOY fixture; the probe host carries adversarial furniture; consequential picks quote code-checkable evidence; real accounts get standing outcome scans (P26 per-user zero-violations).
- **T-CLASS — THE BRAIN HAS A CLOCK** (`lib/utils/user-time.ts`: user tz from their own calendar; `localNow`, `timesInText`): the judge reasons at the USER'S local day+hour (**JUDGE_VERSION 12**); expired needs `expired_on` (< today) or, same-day, `expired_time` — a time STATED in the item, code-verified in-text and behind the user's clock (a 12:30 meeting is over at 20:34, not midnight); the judgment sig gains the event-passed bit. **THE DEIXIS LAW**: stored text never carries decaying day-words — extraction resolves them absolute against the SOURCE'S OWN date (`receivedAt` threaded through sync; `resolveDeixisInDescriptions` = lexical detect + reasoned rewrite, code-checked); entity-state prose bans day-words + treats pre-today ledger events as past (STATE_PROMPT_VERSION 5) + sig gains the passed-calendar-events count. Backfill `scripts/sweep-deictic-titles.ts`.
- **R-CLASS — THE BRAIN HAS A CLIENT MAP** (same people ≠ same deal — the channel-contact law; a partner-org contact brokers several end clients): the recognition judge extracts `named_engagement`; the **NAMED-SUBJECT VETO** (code-side distinctive-token check vs the entity's IDENTITY — name+aliases, deliberately NEVER its summary, which absorbs the intruder's own words) converts a people-matched attach into founding the named work; the **THREAD-DRIFT GUARD** applies the same law at the zero-AI structural-inheritance door (deterministic fast path; one cheap read only when the item never mentions its inherited entity). Repair `scripts/sweep-recognition-subjects.ts`. `namesOverlap`/`GENERIC_WORK_WORDS` = the shared identity-match primitive.
- **UX ARC PART 1 — THE ROOM'S GRAMMAR**: **THE ONE-NARRATOR LAW** (`RoomTurn.author` contract): narration is the CoS voice, author ABSENT; a coworker's avatar/name appear ONLY on their own first-person speech (report-backs/asks). **THREE GRAMMARS derived structurally** in `item-rail.tsx`: user bubble · coworker bubble · muted EVENT LINE (system, no author, no affordance); component turns keep prominent renders (P17); `prep:*` narration FOLDS into the artifact card (keyed on the exposed `dedupe_key`, never text-matched). **ONE COMMIT LINE**: the rail card points (Open →); the stage composer holds the only Send. **SCAPE ORDER on the stage**: composer directly beneath the message card. `clip()` = word-boundary truncation for all narration; turns ordered (created_at, id) + expose `key`. Part 2 (pinned controls strip + stage CTA consolidation) queued post-deploy.
- **Tooling**: `sweep-staged-provenance.ts` (guarded — removes law-violating staged attachments) · `sweep-deictic-titles.ts` · `sweep-recognition-subjects.ts`. ⚠️ Lesson of the arc: while fixes are local-only, the DEPLOYED cron re-poisons every 2h (timestamps proved it) — trust fixes aren't real until deployed; the standing P1/P26 scans catch drift.

### THE DATED-SOURCE LAW (July 30–31 — the workflow-pipeline trust arc; a pilot client caught a 2021 article shipped as current news)

The AHK Executive Briefing (client: AHK Portugal pilot) shipped the Banco de Portugal **June 2021** bulletin as this week's top story — years transposed 2021→2026, citation date fabricated ("Observador, 27.07.2026"). Root causes fixed at every layer; all smoke-verified on real end-to-end runs (test mode) + a 3-scenario **cadence simulation** (last_run rewound 1/7/21 days — zero window leaks, all citations in-window, `last_run_at` restored).

- **Every ingestion path carries its date** (`lib/tools/*`): `fetch_url` stamps `Published: <date>` (URL-pattern + HTML-meta detection) or an explicit UNDATED warning + `max_age_days` drops stale pages. The actual poison: `observador.pt/economia/` **redirects to the 2021 article permanently** (Observador exposes NO working economy feed — `/seccao/economia/feed/` silently serves the site-wide feed; `/rss/economia` also lands on the 2021 article). `rss_feed` gained **`category_filter`** (RSS 2.0/Atom `<category>`, case-insensitive, honest drop-accounting, feeds-without-categories reported not silenced) — the agnostic replacement for landing-page fetches (Observador = general feed + `[Economia, Empresas, Mercados, Energia]`). `web_search` + `deep_research` render Tavily `published_date` ("unknown — do not assume current"). `get_pt_tenders` enforces its day-window **CODE-SIDE** (⚠️ the Base.gov APIBase2 `numDias` param is broken — observed 7,991 rows back to 2014 oldest-first, then 0 rows minutes later) + prints signing AND publication dates.
- **Every AI step has a clock**: `executeAIStep` injects today + the never-modernize rule (a "this week" deliverable is where the model transposes years); same clock in native coworker chat (`lib/work/chat-system-prompt.ts`) and the AgentOS bridge `[TODAY]` context block (the Python worker prompts are static — the date rides `dependencies.user_context`, no box redeploy). DATE DISCIPLINE blocks live in all 6 AHK workflow prompts; `generate-config` rules 7–8 make future generated news workflows born with date discipline + a verification gate + a landing-page warning on `fetch_url`.
- **THE VERIFICATION GATE** (final ai step, all 6 AHK workflows + the seed): the previous step's output is the DRAFT, everything before is SOURCE MATERIAL — deletes/corrects ungrounded claims (caught live: a false "REN majority Chinese-owned" — State Grid holds ~25%; five invented "next week" calendar events), fixes citation URLs, enforces the draft's own style rules, keeps structure EXACTLY (an emptied section keeps its header + an honest empty line, never dropped/renumbered). Requires **`use_worker_identity: false`** (new `AIStep` field honored in `executeAIStep`) — the last-step worker persona + "produce it in your voice" otherwise overrides the keep-the-draft contract and the gate rewrites the briefing.
- **⚠️ LATENT ENGINE BUG FIXED** — `formatPreviousOutputs` head-truncated the final worker-owned step's context at 30k chars (`joined.slice(0, maxChars)`): later pipeline steps **silently vanished** from every worker-owned synthesis (the AHK briefings never cited a single German source; the gate never saw its draft and re-authored). Now 150k with middle-cut truncation — the tail (freshest data, the draft) always survives. **Lesson: never head-truncate a pipeline context; the last steps are the most load-bearing.**
- **Ops findings**: Thorsten's "AHK Mercado Alemão" was **silently dead since July 1** — Together AI 402 at the AI step (tier since moved to Bedrock) AND the failure left `next_run_at` null so the dispatcher never rescheduled it (revived via `computeNextRun`). Three RSS steps had no `since` (= 24h default window on a weekly task — sources only ever contributed Sunday–Monday news); `deep_research` focuses were year-pinned ("…2026" — a slow-burn staleness bug) — all de-pinned. The 6 live workflows = {Executive Briefing, Mercado Alemão} × 3 accounts (Alexandre `08fe4449`, Thorsten `9d3921b2`, `de4e8824`); patch scripts were idempotent one-offs (scratchpad, not committed).

### THE JUDGED ROOM + THE ONE ROOM + THE LABEL FLIP (July 25–27 — the current execution layer)

The execution surface era ON TOP of the one brain. Plans: `docs/judged-room-plan.md` + `docs/one-room-plan.md` (both carry full PROGRESS logs). Memory: `project_judged_room.md`.

- **THE ONE WORK JUDGMENT** (`lib/work/judge.ts` `judgeWork`) — brain-grounded verdict `{work, component, executor, gate, options, resolution, requires, reason}` per item; structural floors first (answered thread, ownership notice law `lib/inbox/notice-demotion.ts` — reads the RAW mailKind so kind-only understandings demote, automated); TIME-AWARE (TODAY in the prompt; `resolution: 'expired'|'answered'` on none verdicts — a past-event ask is moot, an overdue invoice is still work; "answered" ≠ able-to-act: having the means to act is not having acted); component DERIVES strictly from the registry (`componentForWork` — never the model's component half); **STICKINESS through reasoning** — a stale-sig prior verdict anchors the re-judgment prompt ("be consistent unless something materially changed"), SAME-JUDGE-VERSION only (an old-law prior must not entrench what a version bump corrects); cached on `item_plans` kind='judgment', sig = `JUDGE_VERSION:day:activity:pool` (JUDGE_VERSION 6; ≤1 re-judgment/item/day). **The judge is the ONLY gate to preparation** — `prepareOneItem`, the on-demand draft route, the cron's legacy loop, and the nudge route all consult it; no fast-paths. The deck also consults cached plain-none verdicts (`judgedNoneIds` → the ONE demotion set).
- **THE VERDICT MOVES THE POSTURE** (`lib/work/apply-verdict.ts` — ONE consequence module, wired at the pass + the judge serving edge): dispositioned none → resolve (undoable, activity-logged, narrated with the item chip); artifacts contradicting the verdict strip with their narration turns. Sweep: `scripts/sweep-verdict-consequences.ts` (guarded).
- **THE DELIVERABLE RESOLUTION** (July 27, `lib/prepare/requirements.ts` `resolveRequirements`) — `verdict.requires` = the judged artifact INVENTORY (the concrete things the work must include, in the item's own words; reply/send_file/produce only). ONE resolution law before any drafting: per-label retrieval via the universal resolver (pool→KB→drives) + one conservative reasoned batch pick ("related" ≠ "is"); haves STAGE as pool rows (`task_id require:<label>`, first KB-held one rides `draft.attachment`); missing → the room's `input_checklist` ask (`dedupeKey requires:<itemId>`); `artifactTruth` constrains EVERY drafter/producer (claim/attach/build on ONLY what's staged). Wired at every door: pass reply+docsend+produce branches (truth rides the delegation envelope), on-demand draft route, the judge serving edge (`/api/items/judge` after(), settled-guarded), cron loop defers requires-verdicts to the pass. The ingest funnel clears asks (component strips, text stays) + marks the draft stale. **Group-channel ask rules: ONE ask per item** — a coworker's attempted-work ask SUPERSEDES the engine's (both directions); **asks NEVER BLOCK** — work-with-what-you-have contract (delegation prompt + evaluator: a partial deliverable with honest gaps = pass/flag, never `needs_input`) + the checklist's "Go ahead with what's available →" (routes through the one conversation core). Evaluator `needs_input` verdict + `missing[]` = the ask-routing channel (an ask is a conversation event with a checklist, never a "Prepared by" card; no `prepared_by` stamp on asks).
- **Room truth/legibility** — turn dedupe UPDATES IN PLACE (story order is part of its truth); DM-style coworker turns (avatar column + name header; the CoS stays the unlabeled anchor voice); the item chip hides in the item's OWN room (kept in deal rooms); the composer never defaults open on unknown relevance (verdict-first). WorkRow hover controls SAY what they do (Mark done · Dismiss · Add to project — expanding labels); the folder replaces the arrow and opens the ONE picker grammar (search → Start-new-on-top, query pre-fills → tracked-first) → the sticky membership PATCH. Meeting cards open `/item/<tid>?kind=meeting`, never the list. Row project-tags gate at the ONE derivation point (brief `clusterTag` → tracked canonical; slipping cards tracked-only).
- **SYNC SINGLE-FLIGHT** (`lib/email-sync/sync-emails.ts` `claimSync`) — every sync entry point (connect callback's server-side initial sync, the client connect trigger, manual clicks, pushes, cron) funnels through ONE atomic per-connection claim: the /api/connections/sync route claims upfront (its old "mark syncing upfront" IS the claim — inbox polling UX unchanged) and passes `{claimed:true}`; direct callers claim inside `syncEmailsForConnection`. Stale >10min self-releases (a crashed sync never wedges); a losing push's cursor doesn't advance (at-least-once holds). WHY: email dedup held under the connect-era race but RECOGNITION did not — two parallel first syncs founded duplicate entities/persons (55 dup entities + 12 dup items found + merged across users, `scripts/dedup-sync-race.ts`). ⚠️ PostgREST `or()` filters ERROR on UPDATE against `connections` (42703, verified live; fine on SELECT) — the claim is three sequential atomic conditional UPDATEs instead.
- **The notice law refines the noise tier IN classifyItem** — the deterministic no-reply/marketing rule's fyi demotion now consults `isNoMoveNotice` first: a notice the brain says the user OWES (`ownership you_owe` / action-worthy) falls THROUGH to the actionable chain (the Workspace-security-alert deck miss); rules stay authoritative for real verdicts (needs_reply/to_do/waiting_on returned untouched). Same precedence-chain law as everywhere: authoritative → refine → fallback, never an unconditional demotion.
- **THE FIRST LOOK** (new-user arc, `lib/home/first-look.ts`) — the chat is ALWAYS on the Home (the ask zone is never gated behind data); the empty Home tells the truth via the brief's `mail {connections, syncing}` (no mailbox → Connect CTA `/settings?tab=email&section=connections` · first sync in flight → honest syncing card · else the EARNED all-clear; the day ring hides in the first two). Both OAuth callbacks fire a SERVER-SIDE initial sync in after() (never depends on the landing page; **`maxDuration = 300` on both callbacks is load-bearing** — the default budget KILLED a real signup's first sync partway: newest-first storage silently truncated, and the single-flight claim then blocks the client retry until the 10-min stale release. Recovery for a truncated first sync: `last_sync=null` + resync). A connection's FIRST full-window sync fires the one-time bootstrap chain (atomically claimed via `connections.metadata.first_look_at`): `ensureWorkers` (`lib/workers/seed.ts`, shared with /api/workers/init) → `bootstrapMemory` rounds → `runPreparationPass` → bust home_brief — the first real paint is the PREPARED product. JUDGE_VERSION 7 hardening the blank-account gates forced: TODAY states the WEEKDAY (never model-derived); relative deadlines resolve FORWARD from the item's own date; `expired` requires CERTAINTY; `answered` is ONLY for items that ARE closures; and the reason↔work COHERENCE rule (a reason saying something is owed can never carry work=none).
- **THE TIMELINE LENS (July 28)** — ONE event-Gantt everywhere (`components/entities/gantt-chart.tsx`, shared by the Home Timeline lens + the deal room): "By project" = tracked lanes; "Everything" = the same chart + a "Not in a project" band (station-list fallback retired); smart default lands on the tab with content. TWO-PANE frozen layout (⚠️ lesson: an `overflow:hidden` ancestor DISABLES `position:sticky` — the old sticky label cells silently scrolled away inside the collapse wrapper); the full-height pane border is the drag handle (persisted `aug-gantt-label-w`). **THE EVENT TRAIL**: dated action ticks per row (drafted by X · delegated to Y · done — `lib/work-items/gantt-badges.ts` `ganttEventsFor`, ONE shared builder both serving routes call; zero AI; inline status-echo chips rejected — the dot says where things stand, ticks say what happened). Marker honesty: done clamps to today (never future); off-range markers render as edge chevrons with year-aware dates. Instant-feel: route skips reply-reconcile + SERVER last-good cache (item_plans kind 'timeline_cache', 45s staleness, after() converge) + Home prefetch (~300ms) + localStorage hydrate.
- **Tooling**: `scripts/{reset-account-surface,run-prepare-pass}.ts` = the clean-test two-step; `scripts/probe-user.ts` = THE shared probe host every smoke suite resolves (a dedicated auth user — never a real person's account; created after deleting a personal test account broke 12 suites at once); `retitle-commitments.ts` (reasoned title-law sweep, chunked ≤30/call); `sweep-ask-deliverables.ts` (re-review stored delegation deliverables with the CURRENT evaluator) + `sweep-stale-attribution.ts` + `strip-noise-drafts.ts`.
- **Component registry** (`lib/work/surface-registry.ts`): 8 components × `{gate, surface: 'inline'|'stage', when}`. JUDGE_VERSION lives here — bump on prompt/schema change.
- **THE ONE ROOM** — chat-centered execution, one page for all work. `components/room/room-shell.tsx` (BOTH doors — `/item` deep-dive + entity room — mount it: conversation center, STAGE right); `components/room/context-strip.tsx` (the only per-anchor variance: project door / siblings / founding — spatial, never in the stream); durable attributed conversations in **`room_turns`** (migration `20260725_room_turns.sql`, APPLIED — `lib/room/turns.ts` write/read/`roomKeyForItem`: entity room when linked, `<kind>:<id>` loose; keyed dedupe; `author` = coworker attribution). **THE ENGINE NARRATES**: prepare pass, delegation report-backs, verdict resolutions write turns (proactivity = a thread that moved while you were away). Room reset = DELETE `/api/room/turns` + the rail's "Clear" (turns are narration, not memory).
- **Projects are HUMAN-CREATED only** (R4): `tracked:true` written only by POST /api/entities, PATCH track, chat create_project; NO suggestion cards (untracked folds into "smaller things"; discovery = the strip's "connects to X · Track" + ✕ refusal); founding NARRATES existing links as the member proposal (`lib/entities/founding.ts`, zero AI); recognition NEVER founds from noise (kind-aware in `lib/entities/sources.ts`/`recognize.ts`); Timeline lanes tracked-only. Corrections CASCADE: `setItemMembership` re-homes the item's engine turns; `aug:membership-changed` broadcast keeps chip/rail/strip coherent; hygiene sweep `scripts/archive-noise-entities.ts`.
- **THE LABEL FLIP** (`lib/inbox/rules/write-back.ts`): an email carries up to TWO labels — **KIND** (identity, stable: AUGMTD/Receipt·Newsletter·Notification·Calendar·Cold outreach·Customer·Team·Personal; `resolveKind` precedence: `kind_override` → reasoned `understanding.mailKind` → structural headers → none) × **POSTURE** (lifecycle: Needs reply·To do·Waiting on→Done; FYI/Marketing/Notifications posture labels RETIRED — reconciler strips them, NEVER touches kind). `writeBackLabels` pair-applier at sync ×2 + label-sweep (the backstop). Rules typed `set_kind` (override channel). Backfill: `scripts/backfill-mail-kind.ts` (also stamps no-understanding fast-path mail — routing-inert: `coerceUnderstanding` requires role+relevance). Deliverable-shape floor in the evaluator + delegation review (capped retry, honest refusal); nudge + reply drafters mirror the counterparty's CONCRETE words (`mirrorText`); the notice law lives in the SPINE too (`lib/work-items/model.ts` no-move flag).
- **THE PROMISE GATES** (`scripts/smoke-promise.ts`) — standing OUTCOME gates on real accounts + the shared probe host, 76/76: P1 zero drafts on noise · P2 judge-only gate · P3 one obligation = one task (structural fold via `lib/home/dedupe-deck.ts` `isVisibleObligationRow`) · P4 noise never founds · P5 one project definition · P6 label truth · P7 corrections stick (locked refusals) · P8 no show-then-retract · P9 mootness both ways · P10 verdict-posture agreement · P11 deliverable-shape · P12 spine-law parity · P13 user-created-only projects on every surface · P14 founding proposes known work · P15 tracked-only row tags · P16 the kind-only-notification floor + reasoned stickiness · P17 an ask is never a deliverable (evaluator `needs_input`, checklist routing, ingest-clears loop) · P18 the deliverable resolution (live: inventory → room ask → truth-constrained draft; supersession both-ways; never-blocks; one law every door) · P19 the first look (chat always present · truthful empty states · server-side sync on connect · the atomically-claimed bootstrap chain · idempotent worker seeding) · P20 sync single-flight (live: exactly ONE of two concurrent claims wins) + the notice-law refinement of the noise tier (a you_owe notice is never silently awareness; genuine noise stays demoted). **Doctrine: gates assert the PROMISE on served accounts, not plumbing; a fixture hitting engine-correct behavior gets hardened with the same shared checks — the gate is never weakened.** Suites: smoke-{promise,label-flip,one-room,judged-room,work-loop,work-surface,workbench,orchestrated-loop,tasks,room}.ts + `scripts/reset-account-surface.ts` (guarded derived-state reset).

### THE ONE BRAIN — entity memory (July 2026 — the current identity layer; SUPERSEDES the label-era Home-lenses arc, the `projects` table, `initiative_state`, `person_state`, the magnet, and `normalizeInitiative` clustering (kernels kept in "Home lenses era" below))

The core rebuild: instead of compressing understanding into **string labels** at ingest and reconstructing identity downstream by string-matching (the old `normalizeInitiative` keys, grounding clauses, the magnet), the system now has a durable **entity memory** and RECOGNIZES new input against it like a human ("ah, this is the Galp engagement"). Full history in memory `project_one_brain.md`; plan `docs/one-brain-plan.md`.

- **Tables** (`supabase/migrations/20260721*`, `20260720_work_entities_people.sql`, `20260722_work_entities_goals.sql` — all APPLIED): `work_entities` (kind `initiative`|`person`; `name`, `summary`, `aliases`, `state` jsonb, `next_move`, `priority`, `embedding` as jsonb, `people` jsonb fingerprint, `goals`/`rules`, `tracked`, `status`, `sig`) + `entity_links` (item→entity; `via` structural|recognized|user|none, `reason`, `locked`; `entity_id` nullable = a remembered refusal) + `entity_reflections` (pair merge/separate verdicts). **A project IS an entity** — the `projects` table is demolished (drop migration `20260722c`, pending prod verification).
- **Recognition** (`lib/entities/recognize.ts` `recognizeItem`) — IDENTITY-FIRST, topic only a tiebreaker (the key lesson: in a specialist portfolio every deal shares a topic, so topic can't separate them — WHO + WHERE-FROM does). Order: idempotent link check (incl. refusals) → **PROVENANCE** (a derived item — a commitment from a meeting/email — inherits its parent's entity structurally, recognizing the parent inline if unseen; never re-guessed on topic) → **people-first RECALL** (`recallCandidates` scores shared *distinctive* people — rarity-weighted so an internal colleague on everything doesn't count — above topic; a distinctive shared person force-includes the entity) → ONE reasoned **judge** (people are the strongest separator; a 1:1/recurring sync is a CHANNEL not a project — attach to the deal it advances or `none`, never a person-named "X x Y" project) → link with `via`+`reason`. `writeLink` accumulates the entity's people fingerprint. Sources: `lib/entities/sources.ts` (`itemFrom{Inbox,Meeting,Commitment,Calendar}` — a commitment carries `parent{kind,id}` from its `source`/`source_id`).
- **State synthesis** (`lib/entities/state.ts` `refreshEntityState`/`refreshEntityStates`) — sig-gated ONE reasoned pass over the entity's cross-source ledger → `state{summary, momentum, whoOwes, stage, blocking}`, `next_move`, `priority{weight 0-100, reason}` (the REASONED priority that replaced the old verdict weight tables). Category is NOT synthesized here — it's preserved (owned by the grounded classifier).
- **Category** (client|internal|personal|admin) — grounded + **domain-constrained** (a fact constrains the judgment: an external-company person ⇒ NOT internal; own-corporate-domain colleagues ⇒ internal-eligible; the client-vs-personal split stays reasoned). Backfill `scripts/backfill-entity-category.ts`. Lives in `state.category`.
- **Membership RECONCILE** (`lib/entities/reconcile.ts` `reconcileEntities`) — the generalized rule: ANY membership change (a manual meeting/commitment move, attach/detach) re-reasons BOTH the source and destination — recompute people fingerprint from current links, `refreshEntityState`, re-ground category, **auto-archive an entity emptied to 0 members**. Wired into `/api/items/entity` PATCH (captures the source before the upsert; a meeting move CASCADES its commitments — links + legacy `commitment.initiative` — so a manual move can never split a meeting from its own action items; fires reconcile + `logActivity('membership_move', undoable)` + a learning signal in `after()`).
- **Reflection** (`lib/entities/reflect.ts`) — embedding-shortlisted pair merge/separate; conservative by design (never force-merges distinct deals — the trust invariant). Keeper is structural (more links / older). Person entities are an alias registry (one row per human; `scripts/migrate-person-entities.ts` folded `person_state` in — killed the deck alias-dup bug).
- **Live hooks** (`lib/entities/hooks.ts`) — `shadowRecognizeTouched` (sync tail), `shadowRecognizeMeeting` (insights tail), `bootstrapMemory` (chunked, idempotent onboarding for new users). Self-gating on `kind='initiative'` memory count.
- **Consumers** (all read the ONE registry): the Home deck's "Important" weights = entity `priority` (`app/api/home/brief/route.ts`); bundling groups by `e:<entityId>` links (`lib/home/bundle-brief.ts`); the Projects lens = the entity **portfolio** (`components/entities/portfolio-view.tsx` + `/api/entities/portfolio` — reasoned PROMINENCE leads, `tracked` is a human PIN override only, lifecycle verbs, closure suggestions, folded quiet tail, multiselect category filters, "New project" founds a tracked entity); the entity **detail** (`components/entities/entity-detail.tsx` — Overview/Work/Timeline card dashboard + the Gantt); the **Timeline** lens = a project-clustered Gantt (`components/timeline/timeline-gantt.tsx`) with a station-view fallback; the deep-dive deal card + item plan (`lib/home/item-context.ts` grounds on the item's own entity link); drafter/coworker context (`lib/context/brain-context.ts` person-entity + world reads); the meetings sidebar filters to recording-relevant projects, files via locked entity links. Verdict formulas (`lib/brains/verdict.ts` `personVerdict`) survive only as the person_state fallback.
- **Trust gates** (permanent smokes): `scripts/smoke-one-brain-e2e.ts` (14/14), `scripts/smoke-recognition-integrity.ts` (0 provenance violations / no scattered meetings), `scripts/smoke-membership-reconcile.ts` (5/5), `scripts/smoke-briefing.ts`, `scripts/smoke-portfolio.ts`. `lib/ai/call.ts` `aiCall({shape})` is the shape-based router (routes by call SHAPE not a task enum; `bedrock_optimised` = Bedrock-only, Sonnet-capped).

### THE REASONED BRIEFING — the chief-of-staff Home top (July 2026, `docs/home-briefing-plan.md`, memory `project_reasoned_briefing.md`)

The Home's opening is a SHORT BRIEF **authored by the brain** over judged state (not templates): `lib/briefing/compose.ts` `composeBriefing` writes `{lead, action, watchlist, pulse}` around `{A1}`-style refs (names/links swapped in from the registry at render — never guessed). Six laws: judge-not-restate (sees judged state, not raw mail) · say-less-than-you-know (conviction caps length) · never-repeat (yesterday's brief is input) · the-word-is-the-deed (every mention is a live chip) · never-guess-identity · earned calm voice. daySig-gated compose in the brief route's `after()`, stored `profiles.home_brief.briefing`, served last-good. Renderer `components/briefing/briefing-view.tsx` (`BriefingBlock` — plain prose, no card, collapsed to first lines with a measured-height fade + "Read the brief", chips open the deep-dive, struck-through on action). Speaks TO the user (2nd person for their work, "I" only for its own recommendations). `BRIEFING_PROMPT_VERSION` in the daySig so prompt edits recompose.

### Two-environment system

All user-facing routes run on **Vercel** (Next.js 15 App Router, `maxDuration` set per route). Audio transcription runs on a **Hetzner VPS** (`46.224.176.245`) to avoid Vercel's timeout limits. The split is:

- Vercel handles: auth, email sync, AI chat, workflow execution, insights generation
- Hetzner handles: Whisper transcription (`faster-whisper-server:latest-cpu`, port 8000), meeting bot (`infra/meeting-bot/`, FastAPI, port 3001), **AgentOS** (`infra/agentos/`, Agno, port 8001 — see AI workers section)

The `confirm` recording route fires a fire-and-forget POST to `MEETING_BOT_SERVICE_URL/transcribe`, returns 202 immediately, and lets Hetzner call back to `/api/meetings/recording/[id]/generate-insights` when done.

### AI client / multi-tier model routing

**`lib/ai/factory.ts`** is the single entry point for all AI calls. Call `getAIClient(userId, task, supabase)` — never instantiate OpenAI/Anthropic clients directly.

- Looks up the user's `tenant_configs` row (5-min cache)
- Resolves which provider+model to use based on `tier` + task type
- All providers (OpenAI, Anthropic, Azure, AWS Bedrock) are accessed through the OpenAI SDK — they all expose an OpenAI-compatible `/chat/completions` endpoint. Bedrock is wrapped in `lib/ai/bedrock-adapter.ts` to translate requests/responses — **including `embeddings.create`** (`lib/ai/bedrock-embeddings.ts`, Aug 19).
- **THE EMBEDDINGS PRIVACY LAW (Aug 19)**: every tier AUGMTD operates itself (standard · bedrock_private · bedrock_optimised) embeds on **Bedrock EU — Cohere Embed Multilingual v3, 1024-d** (owner call: NEVER Titan — the corpus is EN/PT/DE/FR). Together AI embeddings are gone; no document/chunk/entity summary/item body leaves the perimeter to be vectorised. ONE embedding space platform-wide (the old "KB cross-tier embedding" backlog item dies with it). Cohere is ASYMMETRIC: `embedText(text, userId, sb, { purpose: 'query' })` at the three probe sites (`searchKnowledge`, `knowledge/search.ts`, recognition's item-vs-entity recall); every stored vector is a DOCUMENT; `input_type` rides only on bedrock endpoints. Vectors from two models NEVER share a space (same dims ≠ same geometry) — `scripts/reembed-bedrock.ts` (guarded, `--apply`) regenerates every stored vector through the SAME text derivation the live indexer uses (files ← extracted_text · tier-1 ingest chunks ← raw content · other chunks ← `summarizeChunks` · entities ← `entityEmbedText`) + REINDEXes the ivfflat indexes. Gate `scripts/smoke-embeddings-privacy.ts` (E1 source floor · E2 live routing · E3 SPACE UNIFORMITY — sampled stored vectors must re-embed onto themselves; it FAILS until the sweep has run). **RELEASE ORDER: deploy the routing change and run the global `--apply` sweep in the same breath** — the window between them is the only time search runs cross-model (the sweep was proven on the probe account only; the global run is a release step). Client-endpoint tiers (private_client/on_prem/professional) embed where the client's endpoint says.

Task types: `planning | generation | summarization | classification | embeddings | ocr | assignment | conversation`

Tier defaults live in `lib/ai/defaults.ts`. Production standard tier uses: OpenAI gpt-4o-mini for most tasks, Anthropic Claude for `generation` and `conversation`.

Use `aiCreate(client, params)` (also in `factory.ts`) instead of `client.chat.completions.create()` — it handles 429 rate-limit retries and transient 529/500 errors automatically.

For work with genuinely NO user in scope: `getSystemClient(task)` — standard tier (OpenAI/Anthropic US). ⚠️ **THE TIER LEAK (Aug 19, closed)**: six user-scoped sites (Home brief synthesis · coworker + team briefings · memory rendering · strategy alignment · recipient detection fallback) used it for "background" work, sending privacy-tier tenants' content outside their perimeter. All bind `getAIClient(userId, …)` now (the factory puts the company `ai_tier` first). Gate `scripts/smoke-tier-routing.ts` (17/17): an ALLOWLIST of files that may call `getSystemClient` (each with its no-user reason) + live proof that a real `bedrock_optimised` member resolves to Bedrock on all 8 task types. Adding a caller = adding it to the allowlist with the reason. **NO THIRD-PARTY OSS HOSTS (Aug 19)**: Together AI and Fireworks are REMOVED — no `'together'` provider, no `private_shared` tier (it was Together-only; zero tenants), no `AUGMTD_AI_KEY`/`AUGMTD_FIREWORKS_AI_KEY`, no AgentOS Together client (Python mirror Bedrock-only; its Together branches were dead code). THE RETIRED-TIER GUARD in the factory: a stray DB `private_shared` value (CHECK constraints still accept it — no migration) resolves to `bedrock_optimised`, never to standard (a silent privacy downgrade). Gate T3 in `smoke-tier-routing` (28/28) sweeps lib/app/components/infra/agentos for any host/provider/key remnant. `bedrock_optimised` IS the private shared tier now.

### AI workers — Agno / AgentOS (privacy-preserving agent runtime)

The AI **workers** (Clara/Luca/Max — `custom_agents` rows with `is_worker=true`; **Sofia (content_manager) RETIRED Aug 14** — document production is THE ONE PRODUCTION DOOR's job, her produce-default folded into Clara, rows deactivated via `scripts/sweep-retire-sofia.ts`; **Luca = Branding Expert** — same name/face; role key RENAMED `linkedin_drafter`→`branding_expert` (code + `sweep-rename-branding-role.ts` in one release — the AgentOS bridge routes by DB worker_role, so rows and the box's workers.py ids must agree; render maps keep the legacy alias for persisted turns); specialty: document themes, brand kits, on-brand voice, LinkedIn/social still in scope) run on a self-hosted **Agno AgentOS** service: `infra/agentos/` (Python/FastAPI, port 8001 on Hetzner). Deployed via Docker (`augmtd_agentos` container, `-v augmtd_agentos_data:/data` for durable SQLite sessions). Configured for **private models only** — `infra/agentos/models.py` can only build AWS Bedrock (EU) clients (Together AI removed Aug 19), mirroring the `bedrock_optimised` tier; there is no code path to public OpenAI/Anthropic or any third-party host. Agno telemetry disabled (`telemetry=False` + `AGNO_TELEMETRY=false`). Bearer-auth via `AGENTOS_SECRET` on all routes except `/health`.

**Gated behind `WORKERS_USE_AGENTOS`** (env). When unset → the native hand-rolled loop runs (the fallback, never removed). When `true` → worker chat + workflow `agent` steps route through AgentOS. Flipping the flag is the rollback switch.

- **Bridge:** `lib/work/agentos-bridge.ts` — the `is_worker===true` branch of `app/api/work/threads/[id]/chat/route.ts` proxies to AgentOS `POST /agents/{worker_role}/runs?stream=true`. Translates AgentOS SSE (`RunContent`→text/thinking, `ToolCall*`→tool chips) to the client's event shape, injects per-user context (preferences/memory/identity/routines) via `dependencies.user_context` (+ `add_dependencies_to_context=True` on the agent), persists rich metadata to `work_messages`, fires memory extraction. `runWorkerStepViaAgentOS` does the non-streaming version for workflow `agent` steps.
- **Tools (HTTP pattern):** Python `@tool`s in `infra/agentos/tools_tasks.py` + `tools_data.py` call back to Next.js internal routes `app/api/internal/agentos/{tasks,tools}/route.ts` (bearer-auth + `user_id`), which wrap the SAME executors as the native loop (`lib/tools/*`, `lib/work/generate-thread-document.ts`) — single source of truth, RLS-safe, external keys stay on Vercel. Tools read `user_id`/`agent_id` from `run_context`; box reaches Vercel via `AUGMTD_INTERNAL_URL`.
- **Worker creation/edit:** `app/api/workers/init` seeds the 4 workers; worker `instructions` are not user-editable (customize via `user_preferences`/`memory_text`), so the static role prompts in `infra/agentos/workers.py` always match the DB.

Env (on the box, `/root/augmtd/agentos.env`): `AGENTOS_SECRET`, `AWS_BEDROCK_*`, `AUGMTD_INTERNAL_URL`. Env (Vercel): `WORKERS_USE_AGENTOS`, `AGENTOS_SERVICE_URL=http://46.224.176.245:8001`, `AGENTOS_SECRET`. Redeploy the box with the manual docker sequence (same `ContainerConfig` caveat as the meeting bot — see `infra/agentos/README.md`).

### Supabase clients — two patterns, never mix

- **Server Components / API routes**: `import { createClient } from '@/lib/supabase/server'` — cookie-based session, respects RLS
- **Admin / background work**: `createClient(url, SERVICE_ROLE_KEY)` from `@supabase/supabase-js` — bypasses RLS; only use in API routes, never expose to client

When a route needs to bypass RLS (e.g. reading another user's row), create a separate `adminClient` inline. Many existing routes have both.

### Email sync architecture

Emails flow in via two paths:
1. **Push** (preferred): Gmail Pub/Sub webhook → `/api/webhooks/gmail`; Outlook change notifications → `/api/webhooks/outlook`
2. **Pull fallback**: `/api/cron/fetch-emails` runs every 4h via Vercel cron

Both paths call `syncEmails()` in `lib/email-sync/` which calls `processEmail()` in `lib/ai/email-processor.ts`. This produces an `inbox_items` row with cognitive-cost classification (`action_required | suggested | fyi`).

`ProcessedEmail` interface (as of Phase 187): `workState`, `workTitle`, `signals`, `confidence`, `priority`. Fields removed: `summary`, `keyPoints`, `urgency`, `reasoning`, `preparedOutput`, `whatIPrepared`, `whyMatters`, `draft`. DB columns `what_i_prepared` and `why_matters` are kept but no longer written (new rows get null).

Gmail and Outlook integrations live in `lib/google/` and `lib/microsoft/` respectively. Token refresh is handled inline in each — pass an `onTokenRefresh` callback when you need the new token persisted.

**Sync correctness invariants** (June 2026 — see memory `project_email_sync_bugs.md`). Four bugs caused recurring "missing emails / split threads", all fixed: (1) **Outlook fetch must paginate** the window via `@odata.nextLink` (`fetchUnreadEmails`), not a single `.top()` page. (2) **Only a full-window sync advances `connections.last_sync`** — a push webhook (`options.preloadedMessages`) updates `sync_status` only; otherwise it jumps the cursor past mail the push never delivered. `last_sync` is stamped to `syncStartedAt` (before the fetch). (3) **Email dedup is per-user**: the `existingEmail` / 23505 lookups in `sync-emails.ts` filter by `user_id` — `message_id` is globally unique, so without it a copy synced by another platform recipient blocks this user's own copy. DB enforces this via per-`(user_id, message_id)` uniqueness (`20260622_emails_per_user_dedup.sql`), NOT a global `UNIQUE(message_id)`. (4) **Outlook push webhook must `$select` `internetMessageHeaders`** so `in_reply_to`/`references_ids` populate for RFC thread stitching. Recovery for missing mail: set `last_sync=null` + trigger `cron/fetch-emails` (now race-proof since pushes don't advance the cursor). (5) **Gmail push must advance `connections.push_history_id` ONLY after storage succeeds** (`app/api/webhooks/gmail/push/route.ts` `processGmailPush`, July 6). It used to advance the historyId cursor to the new value BEFORE fetching/`syncEmailsForConnection`-storing the messages — so any storage failure (rate limit, token refresh, timeout, one bad message — all caught+logged) skipped the cursor past unstored mail and the emails were **silently dropped** until the 4h sweep (the real "missed emails" bug). Fixed: advance the cursor only AFTER the sync succeeds; a failure leaves it put so the next push re-fetches the same range (idempotent via message_id dedup) — **at-least-once, not at-most-once**. (Outlook push is per-`messageId` — no cursor to corrupt, not affected.) **Lesson: never advance a delivery cursor before the work it covers is durably committed.** (6) **Outlook pull must look back ≥ syncWindowDays** (`lib/microsoft/outlook.ts` `fetchUnreadEmails`, July 6). It filtered `receivedDateTime ge (last_sync − 3min)` and IGNORED `syncWindowDays` when `last_sync` was set — so when `last_sync` runs AHEAD of the newest actual mail (a push gap, a timing race, or last_sync stamped to sync-START time), the forward-only filter returns 0 and the cursor never rewinds → Outlook silently stops syncing. Fixed: floor = the EARLIER of `(last_sync − 3min)` and `(now − syncWindowDays)`, so the pull always re-checks the window and self-heals a runaway cursor (dedup by message_id makes re-fetch safe). (7) **A stored email must still get an inbox_item — backfill reactivation** (`lib/email-sync/sync-emails.ts` + `lib/inbox/reactivate-on-reply.ts`, July 6). The `existingEmail` recovery branch did `if (orphanCheck) continue`, skipping ALL inbox_item logic when the `emails` row already existed. Because Outlook's PUSH stores the `emails` row first, the next sync always hit that skip → a resolved/dismissed Outlook thread that got a NEW inbound reply could never reopen, and there was no backfill (root cause of "missing Outlook mail"). Fixed: `reactivateOnReply` (inbound-only, only when the incoming message is newer than the item's last-seen received_at, idempotent, non-fatal) — the mirror of `resolveThreadOnReply` — wired into the existing-email backfill paths AND the new-email safety net (one shared helper). **Lesson: a stored email must ALWAYS yield/refresh its inbox_item — never let a dedup skip bypass item creation/reactivation.**

### Inbox classification, AUGMTD labels & auto-draft (Phase 200 — shipped, June 2026)

The **per-inbox rules engine** is the single config surface: rules **classify** (the classification IS the label) and each rule carries `outcome.auto_draft`. See memory `project_inbox_intelligence_v2.md` + `docs/inbox-coherence-plan.md`.

- **Classification** = deterministic-first (`lib/inbox/rules/evaluate.ts`) → AI rule-match (`lib/inbox/rules/batch-match.ts` — uses `aiCreate`, **token budget `chunk.length*90` + fill-missing retry**; under-budgeting truncated the JSON and dropped ids → everything FYI) → heuristics. `bedrock_optimised` classification runs **Bedrock Haiku 4.5** (gpt-oss-120b returned all "none"). `lib/inbox/classify-item.ts` `classifyItem` is the single render-time type resolver (inbox + Home read it).
- **Recipient-aware needs_reply**: `lib/inbox/needs-reply.ts` `isCcOnlyBystander` — CC-only + no processor reply-state → FYI (guards both the `rule_type` branch of `classifyItem` and `isNeedsReply`). **Recipient-role capture (July 8):** `lib/inbox/recipient-role.ts` `computeRecipientRole(to, cc, userAddresses)` stamps `is_cc_only` + the to/cc lists into `source_data` at sync on **ALL** paths, both providers (was only `recipient.position`, which under-populated). A CC-only, not-engaged thread is then **demoted out of `needs_reply` → fyi/awareness** (visible, not hidden) — categorize from the user's seat in the thread. Backfill: `scripts/backfill-cc-only.ts`.
- **Respect per-item `work_state` downstream (July 8 — no keyword heuristics):** `shouldDraftReply` (`lib/inbox/classify-item.ts`) — **never draft for `noted`/FYI**; `lib/inbox/detect-language.ts` — drafts MIRROR the incoming email's language (voice = style only, not language), fixing PT-in-English drafts; keep-an-eye-on excludes `noted` + a grounding clause (no fabrication); action-worthy automated (payment-failed/suspended/security) → re-postured `to_do` (surfaced as an action, not buried, not a reply); FYI digest has a deterministic fallback; commitments "On your plate" (dropped the `source!=='meeting'` filter); an unanswered reply (`lastFromUser===false`) is protected (the AI enrich can't silently drop it; no arbitrary cap). Stale-draft cleanup: `scripts/clear-stale-drafts.ts`.
- **Home-board section routing — understanding is QA BENEATH the rules (July 10):** the per-inbox **rules engine stays authoritative** for classification/labels; the **"understanding"** (`lib/inbox/item-understanding.ts` `{role, relevance, language, bulk}`, produced by `computeUnderstanding` on the **`classification` tier / Haiku** — the big `planning`/Kimi STEP-5 prompt is only the fallback) is a per-item QA/refinement layer BENEATH it, not a competing classifier. Its verdict is consumed by the brief's section routing, the identified-tasks engine, and the drafter — but the understanding itself is just the small verdict; the brief SYNTHESIS is the separate across-board reasoner. **Root bug fixed:** the "For your awareness" gate was an **AND** (`rule_type ∈ {needs_reply,to_do,waiting_on}` AND `relevance='awareness'`) → a correct understanding verdict was discarded whenever the rules hadn't ALSO fired (a real 1:1 with `rule_type=null` stranded in Newsletters). Replaced with a **precedence chain** in `app/api/home/brief/route.ts` (two lockstep spots — the noted-pool split + the `forYourAwareness` merge): **user rule (incl. `type_override`) → understanding acts on its own → fallback**; FYA now merges both pools (actionable + `fyaFromNoted`) and an explicit actionable `type_override` is never demoted. **Recalibrated `action`** to a concrete obligation with a consequence (pay/sign/verify/deadline — not notifications/promos/receipts/calendar-invites): 344→129. **NEW reasoned `bulk` field** (marketing/newsletter/promo/automated broadcast vs real correspondence) — an AI judgment from the body, since header signals (`has_unsubscribe`/sender localpart) are MISSING on most real marketing senders; board `isBulk = understanding.bulk===true OR the header backstop` (backstop only catches automated *transactional* noise the AI's marketing read skips). Re-backfill via `scripts/rebackfill-understanding.ts` (OVERWRITES, calls `computeUnderstanding` directly). **Untouched:** rules engine / `classify-item` / `needs-reply`; meeting/commitment board routing (email-only signal). Read-only cross-user proof: `scripts/smoke-random.ts`. **Lesson:** two classifiers → one decision must join by a **precedence chain (authoritative→refine→fallback), never an AND** (an AND silently disables the refiner exactly when needed); a split headers can't make (marketing vs correspondence) → a **reasoned AI field**, not a keyword list. See memory `project_inbox_intelligence_v2.md`.
- **AUGMTD labels** (`lib/inbox/rules/write-back.ts`): Gmail nested `AUGMTD/X`, Outlook `AUGMTD: X` categories. Master kill-switch `profiles.email_settings.auto_label` (off = in-app identical, just no mailbox labels). Same pattern: `email_settings.auto_draft` master + per-rule `auto_draft` gate drafting; the CC/BCC + standalone draft toggles were removed.
- **Auto-draft**: `app/api/cron/draft-sweep/route.ts` (every 2h) → `lib/inbox/draft-reply.ts` `generateReplyDraft` (voice block + meeting follow-up + rule instructions + a **hard identity anchor** so it signs as the user). Stored on `source_data.draft`; `/api/inbox/[id]/draft` serves it instantly. Home `MustRespondItem` shows "✦ Draft ready" → editable + Send.
- **Home = a dashboard command center** (`app/api/home/brief/route.ts` + `components/home/home-view.tsx`). The pure-prose experiment was **tried and reverted (July 4)** — the Home is a polished **dashboard** (two-zone: main action column + ambient right rail). One **cached** AI pass, `lib/home/synthesize-brief.ts` `synthesizeBrief`, produces the STRUCTURED brief (must-respond replies with who/ask/angle, keep-an-eye-on tier, follow-ups, fyi digest, commitment placements) — NOT prose; it's grounded via `[Rn]/[Kn]/[Wn]/[Cn]` id tags (position-fallback if the model omits an index — a real bug we hit) and reasons about supersession/staleness/grouping over the `lib/home/brief-context.ts` `buildBriefContext` per-person map. **Identity is deterministic (July 7):** a card's `who` ALWAYS comes from the mapped candidate (`cand.from`), never the model's free text — so a row's name/subject and the item it opens can't diverge (fixed a "click card A → open unrelated email B" grounding bug); the model supplies only judgment (why/ask/angle). Applies to both keepAnEyeOn + mustRespond. Cached in `profiles.home_brief` behind a `sig` (today's date + counts + freshest timestamps) + 3h TTL; the FACTS/counts recompute every load.
  - **Live, not stale:** the Home **auto-refreshes** — a background refetch on tab focus/visibility + a 90s poll (`load(true)`, no skeleton flash, resets session dismiss/clear state so restored items reappear). New mail surfaces without a manual reload.
  - **Day-cleared ring** (`DayClearedRing`, top-right): live `cleared/(cleared+needYou)` for today, recomputed per load (outside the cache) + bumped client-side on every action (`sessionCleared`/`clearedIds`), animates up. **Counts by a real resolution timestamp, NOT `updated_at` (July 8–9):** "cleared today" reads inbox via `source_data.resolved_at` (jsonb) + commitments via the `resolved_at` column — stamped ONLY by real resolve paths (inbox complete/dismiss/send-reply/confirm/delete/archive/move, dismiss-sender, reply-resolution; commitment done/dismiss; the commitments-sweep stamps the *fulfilling email's* `received_at`, not `now`) and CLEARED by every reopen/restore (`/api/restore`, reactivate-on-reply, un-mute). Legacy items with no `resolved_at` don't count (conservative); `scripts/rebaseline-resolved-at.ts` (guarded) backfills from a PAST proxy (existing `resolved_at` → deleted/archived_at → created_at — never the polluted `updated_at`). Root cause of the old behavior: `updated_at` bumps on ANY write (sync, label reconciliation, reclassification, maintenance scripts) → previously-resolved items got re-counted as "today" and the ring filled passively (74% with zero user action). Known-remaining: `repliesSentRes` (any sent email today counts) left as-is — scope to inbox-linked replies later.
  - **Per-section live counts + empty states**: each section's count is `server − session-cleared`; a cleared section shows a `SectionCleared` "✓ all handled" state (never-populated ones stay hidden).
  - **Commitment framing** is judged by the synthesis (`commitmentPlacements`: on_your_plate | ball_in_court | informational), overriding the brittle ingest `direction` (fixed the "you owe the refund" mislabel where the user was the requester); structural backstop in `lib/commitments/extract.ts` (a from-user ask can't be `you_owe`).
- **Item deep-dive command center** — every actionable Home item opens a **full-context deep-dive IN the Home** (`/item/[id]?kind=email|meeting|commitment|followup` via Next.js **intercepting + parallel routes** — modal-over-Home on soft-nav, full page on direct visit; back-button/refresh/deep-link work). `components/home/item-detail.tsx` branches on `kind`; the two shells are `@modal/(.)item/[id]` + `item/[id]/page.tsx`. **No more redirects** to `/inbox` or `/meetings`. Reuses shared components: **`components/inbox/thread-messages.tsx`** (the inbox's real thread renderer — avatars, `IframeEmailBody`, collapse — used by BOTH inbox + Home) and **`components/inbox/reply-editor.tsx`** (the inbox's rich `contentEditable` + `FormatToolbar`, shared). Email = thread + rich draft composer (docked sticky); meeting = summary/decisions/risks/action-items; commitment = source context + done/dismiss; follow-up = thread + nudge draft.
  - **Two-column "Identified tasks" panel (July 7):** the deep-dive is a **two-column layout** — MAIN = header + `ThreadMessages` + docked composer/action; RIGHT = the **"Identified tasks"** panel (renamed from "What this takes"), shown ONLY when the plan has **≥2 live tasks** (intent-driven, NOT kind-driven — reverses the earlier kind-gate); single-column at 0–1 tasks. Panel mirrors the Activity-panel reflow (`transition-[width]`, ~300px, flat border-only, no shadow) as a **vertical workflow stepper**: ✦ system / ○ you nodes + connector line, a short title per step + click-to-expand detail, a per-step **dismiss = strikethrough/cross-out toggle (not remove — persists, reversible)**, the [You] done checkbox, "+ add step" + inline edit. Actions are NOT duplicated — the standalone main-column action bar is hidden when the panel is present (the step's Draft→ is the canonical trigger); "Hand to a coworker" lives in the panel footer. Single fetch per deep-dive via a `useItemPlan` hook (was double-fetching); `PATCH /api/items/plan` toggles `done`/`dismissed`. `ItemPlanTask` = `text` (short ≤8-word title), `detail` (one sentence), `done?`, `dismissed?`. The reply composer has **attach (📎)** (reuses the inbox attach path). Actions use **plain labels — "Reply" / "Follow up"** (replaced "Send draft"/"nudge"). Needs-you rows: line 1 = sender · synthesized ask (prominent), line 2 = raw subject (muted). Deep-dive modal + panel flattened (`shadow-xl`→border, no shadow). **Home layout stability:** `RAIL_MIN_FOR_SIDEBAR` 3→1 (two-zone whenever ANY rail content — no content-count-driven one↔two flip) + a latch (`sidebarLatchedRef`, declared BEFORE the loading early-return to keep hook order legal — fixed a hooks-order crash).
- **Activity timeline + undo** — `activity_events` table (`supabase/migrations/20260704_activity_events.sql`, apply manually) logs every action (`lib/activity/log.ts` `logActivity` on the 6 action endpoints, non-fatal). Reached via a **top-right trigger → right-side reflow panel** (inbox-style `transition-[width]` column, content shifts left — `components/activity/activity-panel.tsx` + `activity-timeline.tsx`), NOT a sidebar item. **Undo** (`POST /api/restore` + `lib/activity/restore.ts`): reversible actions only (dismiss/done/mute/commitment-status → status flip; **sends are NOT reversible**). Restore busts the brief cache + resets session state so the item **reappears on the Home**, from both the transient **sonner "…· Undo" toast** and the Activity-log Undo (via a HomeView→panel `onRestored` callback). Log names the specific item ("Restored: <subject>").
- **Thread freshness** (`supabase/migrations/20260703_inbox_items_last_activity_at.sql`, apply manually): a new reply to an existing thread bumps `inbox_items.last_activity_at` (stamped on every sync create/update path); the brief orders/reasons by it (`activityAt` = `last_activity_at || source_data.received_at || created_at`) so a hot thread re-surfaces instead of looking weeks-old.
  - **DIRECTION (locked, `docs/brief-and-labeling-plan.md` + `docs/living-brief-plan.md`):** identity over fuzzy strings; reasoning in the synthesis; evaluate across users not one inbox's screenshots. (Entire July-4 Home arc is on `dev`, `main` clean — pending a review/merge.)
- **Actions follow intent + item work-plans (July 5, `docs/home-actions-plan.md` + `docs/home-actions-stage3-plan.md`):** every deep-dive leads with the natural action, not type-locked buttons. **Universal drafter:** `POST /api/compose/draft` (infers recipient — meeting→attendees, commitment→counterparty, awareness→sender; drafts in the user's voice; NEVER invents a recipient — unknown → empty `to` + name) + `POST /api/compose/send` (sends AS the user via connected mailbox, falls back to `sendCoworkerEmail`). A shared `ComposePanel` (reuses `ReplyEditor`) opens pre-filled. **Stage 2 — "Identified tasks"** (`lib/home/item-plan.ts` `generateItemPlan` + `item_plans` table, `20260705_item_plans.sql`): decomposes an item into 2–5 tasks, each graded **[System]** (draft/analyze/fetch/send — what we can actually do) or **[You]**, **instance-honest** (won't claim "fetch the deck" unless the deck is evidenced — unknown files/recipients become [You] steps). Cached + background pre-gen (top ~4 items on brief load); draft+send collapse to one "Draft & send". ⚠️ **Must use the NON-reasoning `classification` tier** (`getAIClient(userId, 'classification', client)` = Haiku 4.5 on bedrock / gpt-4o-mini on standard) — NOT `planning`. On `bedrock_optimised`, `planning` = Kimi (a reasoning model); the richer `text`+`detail` prompt made Kimi burn its whole budget in the reasoning channel (35k chars, EMPTY content, finish_reason=length) → parse fail → "Handle this" fallback → empty panel. Classification emits JSON directly (~2s, reliable). Build can't catch this; only a real AI call does — `scripts/test-item-plan.ts`.
- **Identified-tasks EXECUTION engine (July 7, `docs/identified-tasks-execution-plan.md`) — the plan is now a grounded, executable, editable workflow, not a static guessed list. Built agnostically off a capability map so adding a tool/capability/coworker touches NO classifier/executor/UI code.**
  - **Capability map** (`lib/home/capability-map.ts`) is the single source of truth: each capability `{intent, tool, built, kind:'atomic'|'judgment', irreversible, feature}`. `renderCapabilitySet()` DERIVES the classifier prompt from the map, so adding a map entry updates classification automatically. `PLAN_VERSION` constant lives here — **bump it whenever the map or classifier prompt changes** (auto-invalidates cached plans, see below).
  - **Classifier reads the map** (`lib/home/item-plan.ts`): removed the hardcoded "calendar → [You]"; a **one-action-per-step** rule (never bundle a system action with a you action). `classifyStep()` is the reusable single-step engine (used on user add/edit), same map + `GRADING_RULES` as `generateItemPlan`.
  - **`send_calendar_invite` tool** (`lib/tools/send-calendar-invite.ts`) — promoted the previously-siloed `lib/calendar/invite-sender.ts` (real Google/Outlook invites) into the tools registry so it's classifiable + callable; registered in `TOOL_FEATURE` under `meetings`.
  - **Auto-invalidation** (`item_plans.version` column — `supabase/migrations/20260707_item_plans_version.sql`, APPLIED): `POST /api/items/plan` regenerates a stale-version (`< PLAN_VERSION`) plan — no more manual cache-busts.
  - **Editable plan:** `PATCH /api/items/plan` `action:'add'|'edit'` → `classifyStep` re-interprets; "+ Add a step" + inline edit in `item-detail.tsx`.
  - **Prepared execution (prepare → approve-before-commit → execute):** `POST /api/items/prepare` (grounded, NO side effects) + `POST /api/items/execute` (the ONLY place a real invite fires, on explicit approve) + `InvitePreviewCard` (editable prepared invite). Marks the step done + logs activity. Verified LIVE (real invite sent). Email steps reuse the existing `ComposePanel`.
  - **Coworker delegation:** `POST /api/items/delegate` (`{kind, entityId, agentId, taskId?}`) — "Hand to a coworker" at item + per-step level; reuses `executeAgentStep` (flag-agnostic, exported from `lib/workflows/execute-step.ts`, works whether `WORKERS_USE_AGENTOS` is on/off) + `lib/home/delegate.ts` builds the prompt; report-back via `lib/workflows/report-back.ts` into the coworker's chat thread + activity + on-item `handedTo` attribution. Safety = prompt-level guardrail (prepare + report, not auto-send).

#### Task-workflows execution engine (July 7–8, `docs/task-workflows-plan.md`)

The "Identified tasks" panel became a **board of per-task mini-workflows sharing a per-item deliverable pool** — the substance *underneath* the July-7 surface (capability map / classifier / prepared-action UX above). The model: **each step is its own small workflow** (deliverable + owner + tools + reasoning), NOT one monolith for the whole item. **Load = cheap identify** (`generateItemPlan`, unchanged); **RUN = assemble the step into a real engine workflow just-in-time.** Context flows via a **per-item deliverable pool**, not a hardcoded dependency graph — a step reads prior deliverables at run time; order is the user's; running an upstream step enriches downstream; missing inputs self-heal-or-ask.

- **S1 — pool + system-step assembly:** `item_deliverables` table (`supabase/migrations/20260708_item_deliverables.sql`, **APPLIED** to the live DB) is the pool (user/kind/entity-scoped, typed rows: text|document|file|sent|draft, links out to `work-artifacts`/`knowledge_files`). `lib/home/deliverable-pool.ts` (`readPool`/`writeDeliverable`/`renderPoolForContext`). `lib/home/assemble-step-workflow.ts` `assembleSystemStep` — a system step runs a **real tool** via `executeStep` (reuses the Studio engine's dispatcher, NOT `runWorkflow`'s thread/run-row machinery) and writes its output as a pool deliverable. Send capabilities are **REFUSED** by the assembler (approve-before-commit stays with the prepare→execute gate). `POST /api/items/run` calls it (replaces the old tool-less single-LLM `runItemStep`).
- **S2 — coworker reads + writes the pool:** `lib/home/delegate.ts` `runDelegation` now reads the pool into the coworker's `previousOutputs` (closes engine-gap #1 — was `previousOutputs: []`) and writes the coworker's output back as a deliverable. So context flows system→coworker→coworker (e.g. Luca drafts from Max's brief) with no explicit dependency wiring.
- **S3 — request-attachment from the user:** a new `awaiting_input` step state + `task.request` (the ask); `detectAttachmentRequest` grades an unevidenced provide-a-file step as [You]/awaiting. `POST /api/items/attach` mirrors chat-attach (upload → `text-extractor` → index) → lands a `file` deliverable (with extracted text) in the pool. The panel renders "Upload →" for such a step; downstream steps then use the file.
- **S4 — smart file resolution (REASONED, not keyword):** `lib/home/resolve-file-step.ts` `resolveFileForStep` — **pool-first** `have_it`; else **semantic KB search** (reuses `searchKnowledgeGrouped`, NO doc-noun vocabulary) + **ONE cached `classification`-tier reasoned pick** over the top ≤5 candidates → `found_one` / `found_many` / `none`. Cost-controlled: lazy (only on engage), pool-first short-circuit, cached on the step (`resolvedFile` snapshot keyed by step text + pool sig), cheap tier only. `POST /api/items/resolve-file` + `POST /api/items/use-file`. Confirm / disambiguate / ask UI; the 📎 attach affordance is always available as a fallback.
- **S5 — future-tool owner flip (the agnostic proof):** `forward_email` tool (`lib/tools/forward-email.ts`) + **ONE `CAPABILITY_MAP` row** → the SAME step regrades [You]→[System] with **zero edits** to `proposeOwner`/the classifier/the assembler (the invariant, proven by construction). Prepared `forward` action + `ForwardPreviewCard` (mirrors `InvitePreviewCard`). Recipient inference is **literal-email-only** — never invents an address.
- **Run-the-plan PANEL model:** each step renders **owner chip · state · one action**. `proposeOwner` (in `lib/home/capability-map.ts`) suggests **AUGMTD** (atomic system tool) / a **coworker** (judgment/draft) / **you**. One hero **Run** button walks the plan: system steps run inline, coworker steps dispatch, send steps pause for approval, you-steps pause. Consolidated footer. **The owner menu is ASSIGN-ONLY** (`proposedAgent` field): picking a coworker sets the proposed owner, it does NOT run — **assign ≠ run**; execution happens only on Run. Panel polish: task-icon header (the AUGMTD mark stays on per-step owner chips), right-flush layout (mirrors the Activity panel), plain labels "Reply" / "Follow up"; the bulk-delegate link was dropped.
- **The agnostic invariant (locked):** adding a capability = **one `CAPABILITY_MAP` row + a registered tool + (for sends) a prepared-action card** — the classifier, owner proposer, and assembler are untouched. S5 is the regression test for this. Other locked lessons: each step is its own workflow (never a monolith); reason on RUN not on load; the deliverable pool is the context mechanism (no hardcoded dep graph); reuse `executeStep`/tools, NOT `runWorkflow`; file resolution is REASONED (semantic search + one cached cheap pick), never a keyword vocabulary.
- **Home brief RELIABILITY (July 7, `app/api/home/brief/route.ts` + `lib/home/synthesize-brief.ts`):** membership is deterministic, the AI only enriches — **the enrich pass can reorder/enrich but CANNOT drop a protected item.** (1) **Vanishing-reply fix:** a genuinely-unanswered inbound reply (`lastFromUser===false`) is PROTECTED — was shown on basic load then silently dropped on enrich; the drop-filter is now gated by the deterministic reply-state. (2) **No arbitrary cap** on must-respond (`slice(0,25)`→100 headroom). (3) **Automated-sender handling:** `isAutomatedSender` (no-reply/transactional) keeps automated mail OUT of `needs_reply`, BUT `isActionWorthyAutomated` (action/decision-required, or payment-failed/account-suspended/security signals) **re-postures it to `to_do`** so an actionable notification surfaces as an action card — NOT buried in FYI, NOT a reply ("can't reply ≠ no action"). (4) **FYI digest never blank:** `buildDeterministicFyiDigest()` fills from groups when the AI omits the block. (5) **Commitments "On your plate":** removed the `source!=='meeting'` filter that hid ALL meeting commitments; expandable lanes (`ExpandableRows` "Show N more") instead of hard caps.
- **Commitment QUALITY (July 7, `lib/commitments/extract.ts` + meeting-insights prompt in `bot-manager.ts`):** extractor tightened to **explicit obligations only** (not granular sub-tasks), **due_date only when the source states one** (no fabrication), **capture counterparty**, **write-time dedup** (`isNearDuplicate` — Jaccard content-token overlap 0.6). **Alias-aware 1:1 counterpart resolver** (`sameAttendee`/`emailDenotesName`): collapses an email + display name (and name variants) of the same person, strips the user in any form → resolves the 1:1 counterparty through mixed aliases; null (source label) only when it can't reduce to one person. **Display:** null counterparty → source label ("from {meeting}"), NEVER the literal "Someone"; route-level dedup backstop. Cleanup via `scripts/dedup-commitments.ts` (dry-run default).
- **Live Home (realtime, July 5):** a Supabase **realtime subscription** on `inbox_items` + `commitments` (user-filtered) → debounced (~2.5s) `load(true)` background refetch, so new mail surfaces in seconds (focus + 90s poll stay as backstop). A small **sync-status** indicator in the header (● emerald live dot / "Syncing…" / "Updated Nm ago"). ⚠️ Requires TWO manual migrations: `20260705b_home_realtime.sql` (adds the tables to `supabase_realtime`) AND `20260705c_realtime_replica_identity.sql` (**`REPLICA IDENTITY FULL`** — WITHOUT it a `user_id`-filtered realtime subscription delivers NOTHING; the filter column must be in the WAL). Smoke-proof: unfiltered events fire, filtered don't, until replica identity is FULL (`scripts/smoke-final.ts`).
- **Reply/closure resolution (July 5 — agnostic, structural, NOT keyword-matching):** `lib/inbox/thread-resolution.ts` `computeThreadReplyState` (pure, direction+time only — a user-sent message after `since` IS a reply; NO keyword/regex, language-proof) + `lib/inbox/resolve-on-reply.ts` wired into the sync's `is_from_user` branch: when the user has replied on a thread (incl. externally from Outlook/Gmail — Sent folder + thread backfill carry it as `is_from_user`), the open **needs-reply item → completed** + the **`you_owe` commitment → done** (leaves `awaiting`/ball-in-court alone — a your-reply doesn't fulfill what THEY owe). Logged (`marked_done`/`commitment_done`, "Resolved (you replied)") + **undoable** via `/api/restore`; busts the brief cache. **Shared context (not a siloed flip):** the reply-state (`userResponded`/`lastFromUser`) rides in `buildBriefContext` + the synthesis reasons over an "ALREADY RESPONDED → drop it unless a newer inbound reopened it" dimension (the fuzzy counterparty-closure case via REASONING, not keywords). Migration `20260705d_commitments_resolution.sql` (resolved_reason/resolved_at — **APPLIED**; the day-cleared ring's commitment half depends on `commitments.resolved_at`; resolver still falls back to a status-only flip where absent). Closes the "don't nag me about things I've already handled" loop. **Verified on real data** (controlled resolver test → `{resolvedItems:1, resolvedCommitments:1}`, logged + reversible).
- **THE UPGRADE LAW (July 31 — fallback-finality flaw, found live: receipts wearing "Notification")**: a STRUCTURAL-fallback kind is a PLACEHOLDER, never a final verdict. The stamp records the TIER (`kindTier` in write-back.ts; sync stamps `labeled:'fallback'` for fallback kinds — only reasoned/override stamps `true`); the label-sweep works fallback-stamped items when its reasoning budget allows (never re-stamps the same guess as final); **ONE KIND LABEL** — applying a kind strips every other kind label (Gmail thread peek-strip + Outlook `removeOutlookCategory`), so a reasoned Receipt REPLACES the fallback Notification. `computeUnderstanding` accepts `opts.facts` (facts constrain judgment — the category-grounding law); `ensureMailKind` passes the own-coworker registry fact (`@team.augmtd.ai` → the user's own assistant → 'team', never third-party noise). Repair: `scripts/sweep-kind-upgrade.ts` (guarded, `--days 7` default — older fallback labels aren't worth the spend; drained 338 items across users, receipts/team verified). Gates U1–U4 + two live fixtures (smoke-label-flip 26/26).
- **Labeling reliability — LABEL TRUTH (July 27)**: `writeBackLabels` returns a tri-state **`'applied'|'noop'|'failed'`** — the old boolean recorded "nothing to apply" as success, so fresh TRANSACTIONAL mail (a Canva invoice: fast-pathed → no understanding, no List-Unsubscribe → no resolvable kind) was stamped `labeled:true` with ZERO labels and the sweep (whose work-list is unstamped items) skipped it forever (684 poisoned rows found across 4 users). Now: the sync stamps ONLY on `'applied'`; **the sweep is the KIND COMPLETER** (`lib/inbox/ensure-mail-kind.ts` `ensureMailKind` — computes the reasoned mailKind, merge-only, routing-inert; capped 40/user/sweep) so the missing kind LANDS instead of waiting for an understanding nothing computes; computed-but-null stamps FINAL (terminates), budget-exhausted noop is revisited, failed retries. **THREAD-SCOPED RECONCILE**: applying the pair strips every OTHER state label from the Gmail thread (`GmailLabelCache.peek` — lookup-only, never creates) — Gmail's thread row shows the UNION of message labels, so stale/retired postures (FYI/Notifications/Marketing) stacked next to current pairs until this. **`app/api/cron/label-sweep`** (every 2h, throttled) is the eventually-consistent backstop. Tooling: `scripts/unstamp-poisoned-labels.ts` (reset stamps whose pair resolves to nothing) + `scripts/run-label-backfill.ts` (the sweep's exact flow, 7-day window, all users) + `scripts/refresh-default-rules.ts` (re-seed every connection's default-sourced rules to the current catalogue; user-created rules untouched — NOTE: stored rules are snapshots, a future catalogue change needs a re-run or a versioned auto-refresh). Gates: smoke-label-flip 20/20 incl. the live noop probe.
- **Inbox simplification (July 6):** the inbox is now a **single chronological mail list** — the Standard/Smart view toggle was removed (the "Smart" grouping collapsed everything into one "Noted" bucket because classification defaults to `fyi`/`noted`; the **Home** owns triage now). Dead code removed (`email-list-sections`, orphaned `inbox-view-toggle`); the toolbar is a folder/count header. **Default-open email is always from the selected inbox account** (`inbox-page-client.tsx` — auto-select filters by `selectedConnectionId`, re-selects on account switch, never injects a cross-account item).
- **AUGMTD label reconciliation (July 6)** (`lib/inbox/rules/write-back.ts` `reconcileAugmtdLabel` + `lib/inbox/reconcile-item-label.ts`, Gmail + Outlook): labels were write-once at classify (add-only, went stale). Now they RECONCILE on state change — **swap to `AUGMTD/Done` on resolve** (all four paths: tool reply / Done / Dismiss / external Gmail↔Outlook reply via `resolveThreadOnReply`), and **re-label to the active state on reactivation** (`reactivateOnReply`). Honors `email_settings.auto_label`; non-fatal; **Dismiss maps to `AUGMTD/Done`** (no separate Dismissed label). Both providers smoke-tested against the real APIs.
- **Home two-zone layout latched (July 6)** (`components/home/home-view.tsx`) — the main+rail layout only expands false→true (never contracts), so the optimistic basic→enriched load can't cause a one-column→two-column flip.

✅ **Sync regression FIXED (June 28)**: the main email-store path (`sync-emails.ts` ~786) didn't strip `has_unsubscribe` → every `emails` INSERT failed with PGRST204, swallowed by try/catch → NO mail stored since ~06-25 (the real cause of "no new mail / labels not applied / brief stale", both providers). Fixed. Also: Gmail fetch `category:primary`→`in:inbox` (was skipping Promotions/Updates); AUGMTD write-back added to the **fast-path** (fyi/noise) so newsletters/promos label too. **Lesson:** the same parser-only field is stripped in 3 separate destructures (lines 365/786/1620) — adding a field means updating ALL three; prefer a single emails-column allowlist.

**Home per-item actions (June 30)**: Done/Dismiss on EVERY Home brief section + "Show N more" expand + smooth fade-out. New endpoints: `PATCH /api/commitments/[id]` (done|dismissed) and `POST /api/inbox/dismiss-sender` (mute a sender's awareness mail). Actions persist on `inbox_items.status` / `commitments.status`; `classifyItem` hides completed/dismissed so they never resurface. See memory `project_inbox_intelligence_v2.md`.

### Home lenses era (July 10–14, label-era — SUPERSEDED by THE ONE BRAIN + the judged deck; kernels + still-live pieces only)

The July 10–14 arc built Home lenses on string-label clustering (`normalizeInitiative`, the `projects` table, the magnet, `getActiveInitiatives`) — all replaced by the entity registry. What SURVIVES:

- **The work-item SPINE** (`lib/work-items/model.ts` `buildWorkItems`) — read-time normalizer (no new table) mapping commitments + actionable inbox items + deliverables → `WorkItem`; `timeframe.ts` `inferBucket`; **an undated item can NEVER be "overdue"** and **an automated notice can never be "overdue"** (both locked). The view-switcher island (`components/home/view-switcher.tsx`, `?view=` via replaceState).
- **Dev-server fix (operative)**: `next dev` uses a SEPARATE `NEXT_DIST_DIR=.next-dev` dist dir (`distDir` in next.config.ts, `.next-dev/types` in tsconfig) so dev + build never share `.next` output; `predev` rm -rf .next-dev. This ended the recurring `.next` corruption.
- **Locked lessons**: reason ONCE at labeling (AI), match cheaply at grouping (deterministic) — entity resolution beats freeform clustering (now embodied by recognition). Projects (`tracked`) are HUMAN-created only; delete/un-group returns items loose, never destroys work. NO real names in code/prompts/placeholders (hard rule). A same-visual = same-meaning UI law. The sticky-footer needs the flex `mt-auto` chain (gap-*, not space-y), never negative margins.
- **TRUST law (live in the brief route)**: a direct-recipient `needs_reply` is NEVER silently hidden — the awareness demotion applies ONLY when the user is CC-only (`sd.is_cc_only===true`); rules are authoritative, the understanding refines, it never overrides a reply you owe. (Two classifiers → precedence chain, never an AND.)
- **Understanding schema (live — the judge/brief read these)**: the one Haiku pass also extracts `deadline` (absolute, anchored to received_at, never invented), `ownership` (you_owe|awaiting|none), `effort`, `confidence`, `bulk`, `initiative(_via)`. `fetchAllRows` (`lib/utils/fetch-all.ts`) fixes the silent 1000-row PostgREST cap.
- **Person-bridge lesson (permanent)**: an internal / same-corporate-domain person is NEVER a valid person-bridge — they attend everything (the 47-meeting Galp swallow). And labeling IS entity resolution: ground new labels in the counterparty's established canonical, never blind-then-reconcile.
- **Mute revival law**: a muted grouping revives only when something ACTUALLY ARRIVES post-`muted_at` (ingest time `lastArrivalAt`, never future meeting start-times). `muted_initiatives` table remains; un-mute via /api/restore.
- **Meetings as project context**: `meeting_transcripts.initiative` + `project_id` columns (+ knowledge_files propagation) — a meeting joins work like any other atom; manual membership outranks auto, permanently (`project_locked` columns). Auto fills blanks; a human decision sticks.

### Read-time reply RECONCILE + instant-load (July 13–14 — the parts that remain core)

- **Read-time reply RECONCILE (trust, live)** — `lib/inbox/reconcile-replied.ts` `reconcileRepliedItems`: re-derives reply state from the ACTUAL thread at READ time and resolves any "reply needed" item the user already answered (a one-shot sync event WILL miss; resolution is re-derived at read, never trusted to a moment). Resolver eligibility = the spine's union (`work_state IN reply-states OR rule_type='needs_reply'`, waiting_on/fyi override guarded). Wired into `buildWorkItems` + the brief route (busts `home_brief` only when it resolves); `skipReconcile` for purely-visual reads; `skipLabelReconcile` on hot read paths. Commitment closes now pass THE FULFILLMENT LAW first (see the proactive-team section).
- **Plan ordering**: `orderCommunicationLast` in `lib/home/item-plan.ts` — communication/commit steps sink to the END (prompt rule + PLAN_VERSION bump self-corrects stale plans).
- **Client-safe module law**: importing a runtime value from `lib/work-items/model.ts` into a client component drags the server graph (`fs`/`net` build error) — client files import TYPES only; pure helpers live in `lib/work-items/gantt-date.ts`. Bump versioned LS keys on ANY shape change (a stale blob NaN-crashed the Gantt; guard NaN anyway).
- **Instant-load doctrine (platform-wide)** — `lib/utils/local-cache.ts` `loadLS`/`saveLS`: hydrate last-known → background refresh → persist; gate skeletons on `loading && !cached`; for SSR pages move slow external calls OFF the blocking path (inbox folders); moving data SSR→client can null values other logic assumed at mount — audit consumers. STAMPED July 30: action surfaces demand freshness (`maxAgeMs`); ambient stays ageless. SSR'd routes read caches in effects, never useState initializers (hydration mismatch).

### Bundling + the deck (July 14 — mechanism live under the judged deck)

- **Server-side bundling** (`lib/home/bundle-brief.ts` `computeBundles`): actionable atoms group by INITIATIVE (now entity links `e:<id>`) → MEETING → THREAD, ≥2 threshold, two-pass; deterministic, NO freeform AI clustering. **Reasoned naming** (`lib/home/name-bundles.ts`): conservative classification-tier pass names bundles + a grounded one-line "why" ONLY when a real fact grounds it; cached in `profiles.home_brief.bundleNames` keyed by bundle-set signature, refreshed in after() — the response never waits on the AI. ⚠️ Bedrock Haiku ignores `response_format:json_object` and fences JSON — strip fences + slice to the outer `{…}` before parsing.
- **Deck lessons (locked)**: a true horizontal carousel was tried and REVERTED (variable heights + scan-and-clear fight sideways flipping); progress is owned singularly by the day ring. Framework grounding applied: JTBD (bundle by job/context), overview→filter→details, focus+context, information scent. Modal-nav: the `@modal` slot self-dismisses via `usePathname()` when the URL leaves `/item/…`.

### Studio workflows

Defined in `lib/workflows/types.ts`. A workflow is a linear pipeline of steps:
- **`tool`** — deterministic fetch (RSS, web search, email, meetings). All tools registered in `lib/tools/index.ts`
- **`ai`** — inline prompt transformation. `model_tier: 'fast'` → summarization task, `'reasoning'` → conversation task
- **`agent`** — delegates to a `custom_agents` row, injecting its system prompt, KB files, and memory

`lib/workflows/execute-step.ts` dispatches each step type. `lib/workflows/run-workflow.ts` orchestrates the full pipeline and handles streaming output to the run's linked `work_thread`. When `WORKERS_USE_AGENTOS` is on, `agent` steps route through AgentOS (`runWorkerStepViaAgentOS`); `tool`/`ai` steps and all output materialization stay in TS.

Cron dispatch: `vercel.json` schedules `workflows-dispatch` hourly; it calls `runWorkflow()` directly via `after()` (fire-and-forget safe from Vercel function shutdown).

**Studio is builder-only (no overview page).** Tasks are created/managed conversationally and from each worker's Tasks tab; the standalone Studio grid/overview + detail panel and the legacy `/studio/[id]` + `/work/studio/*` routes were removed. `/studio` is now solely the pipeline builder (`components/work/studio-builder.tsx`), reached as a deep-dive: `?workflow=<id>` edits a task's steps, `?assign_worker=<id>` creates a blank one, bare `/studio` → redirects to `/workers`.

**Coworker-centric builder framing** (studio-builder.tsx): step types relabeled — `tool` = Tool step, `ai` = "Write / produce", `agent` = "Hand off to a teammate". Tool picker grouped **Gather / Collaborate / Act** (NB: TWO group lists in the file — `TOOL_GROUPS` for the top "+" popover *and* `InlineToolGrid`'s own list for the side panel — keep both in sync). `get_workflow_output` relabeled "Use a coworker's task". `linkedin_post` **deprecated** from the picker + `generate-config` (superseded by the LinkedIn coworker + a LinkedIn skill) — still in `AVAILABLE_TOOLS` + `execute-step` for back-compat. A task is reusable structure that runs **on a schedule OR on demand** (`run_task`) — the chat prompt block is `[TASKS]` (not "recurring").

### Task output / delivery model (single home + coworker report-back)

`lib/workflows/types.ts` → `normalizeOutput()` is the single source of truth mapping any (legacy or new) `output_config` to a runtime shape. A deliverable has ONE **home** (`destination`): `message` (run thread), `document` (Drive/Documents artifact), `slack` (a channel or `@me` DM), or `email`. The app's run record (Activity) is always the ledger; the home is where the content lives. Documents may also **link-out** (`link_out.slack` → an instruction-driven announcement, written by `composeSlackMessage` in `lib/workflows/slack-message.ts`). Legacy `thread_message`/`artifact` + `notification_mode` still read via the normalizer (no migration).

After a run, the coworker posts an **AI report-back** (`lib/workflows/report-back.ts`) — a first-person "DM from a colleague" grounded in the run facts (what/where/link/next + a proactive step, uses the user's first name). It becomes the in-thread message (for non-`message` homes) + a `workflow_notifications` card (sender = the coworker), surfaced as a **DM-style feed** ("From your team") on team-home (`/api/workers/home` `messages[]`). `report_mode` (`each_run`|`digest`|`silent`) supersedes `notification_mode`. Optional real Slack DM of the report-back (`profiles.slack_dm_reports` — `supabase/migrations/20260620b_slack_dm_reports.sql`; `sendSlackDM`). Builder: `OutputEditor`; chat: `update_task` `output_destination`/`output_slack_channel`/`output_report_mode`/`output_slack_announcement`. A trailing `slack_send` step is a side-effect — `run-workflow` excludes send steps when picking the deliverable.

### Integrations — self-hosted Nango + per-coworker Slack apps

Connections run on **self-hosted Nango** (`https://nango.augmtd.ai`, on the Hetzner box behind Caddy; `/root/nango/`) — OAuth token custody stays on our infra. `lib/integrations/nango.ts` (fetch wrapper: connect sessions, `/connection`, `/proxy`), `lib/integrations/connection.ts` (scope-aware resolution + per-worker tool settings), `lib/integrations/registry.ts` (catalogue). Connect flow: `app/api/integrations/connect-session` → `@nangohq/frontend` `new Nango({host}).auth(provider)` = **direct OAuth popup** (NOT the Connect-UI iframe — that needs a Nango login, wrong for end users). Confirm via **server-authoritative** lookup in `app/api/integrations/[provider]` (`listConnections` by end_user; Nango assigns its own `connection_id`, mapped to our company via `end_user_id` — store THAT, not the scope key). Writes via delete-then-insert (the unique indexes are partial → `upsert(onConflict)` fails). `integration_connections` keyed by `company_id` (company scope) or `user_id`. Env (Vercel): `NANGO_HOST`, `NANGO_SECRET_KEY` (the **Prod** "Default - Full access" API key for account `alex@augmtd.ai`, env 3).

**Slack = one app per coworker** (distinct bot identities → separate DM threads + real `@mentions`; validated as the right pattern by Salesforce's own SlackAgents). Each `worker_role` maps to its own Nango provider key (`SLACK_APP_BY_ROLE`: `slack-clara`/`slack-luca`/`slack-max` — `slack-sofia` retired Aug 14, app deleted owner-side); `getIntegration()` resolves these as the company-scoped Slack def. `lib/tools/slack.ts` routes every call through the worker's own app (`slackConn` by role) — **no persona override** (each bot IS the coworker). Tools: `slack_list_channels`, `slack_post_message` (`<@Name>`/`<@me>` mentions resolved against real members; `thread_ts` for thread replies), `slack_read_messages` (+ `days` window), `slack_list_members`. Pipeline steps: `slack_read_channel` (source) + `slack_send` (instruction-driven action). The one Settings card connects the 3 apps **one-per-click** (browsers block sequential OAuth popups). **Channel posts carry an attribution label** — a Slack `context` block (`👤 {firstName}'s {role}`) so a shared bot ("Clara") is traceable to the right person (one bot per role is shared company-wide via the company-scoped connection); DMs get none (already per-user threads). Applies to chat posts, `slack_send`, and document announcements — all funnel through `executeSlackPostMessage`. **Per-coworker default target** (Tools tab) is a selector — **DM me** (`@me`) / **Channel** (`#name`, auto-prefixed) / **Channel link** (paste a Slack URL); `normalizeChannel` extracts the channel ID from a pasted `/archives/<id>` URL **anywhere** a channel is given, so a dedicated **private** channel works (invite the bot, paste the link). Stored in `agent_tool_settings.config.default_channel`. (Tool-result success strings: `Posted…`/`Sent…`/`Replied…` — the chip summary must match all three or a DM is mislabeled "failed".) **Inbound/two-way is NOT built** — the future "group thread in Slack" surface (the natural multi-coworker collaboration room). The legacy single-bot persona model was replaced by this.

### Per-worker tools + cross-coworker awareness

**Per-worker tools:** `agent_tool_settings` (`supabase/migrations/20260620_agent_tool_settings.sql`, apply manually) — `(agent_id, provider, enabled, config)`, no row = on. A "Tools" tab per worker (`worker-tools-tab.tsx`, `GET/PUT /api/agents/[id]/tools`): toggle a connected tool per coworker + per-tool config (e.g. Slack default channel). Gating lives in the TS executors (`isToolEnabledForAgent`) + the `[CONNECTED INTEGRATIONS]` context filter — both chat paths, no AgentOS redeploy.

**Cross-coworker awareness** (`lib/tools/team-work.ts`): `find_team_work` (find a teammate's recent outputs by topic/coworker name; matches title **and** document content) + `read_team_work` (read one — scans the coworkers' thread artifacts in JS, NOT a jsonb `contains` query, which is unreliable for partial-object matches). Lets a coworker build on a teammate's work without the user routing — *"Luca, write a post from Max's research"* → Luca pulls it himself. Reads the user's own coworkers' thread artifacts (RLS-safe). `[TEAM]` chat prompt: talk to the output owner; they pull the teammate's work. Wired native + AgentOS + Python (`tools_data.py` `DATA_TOOLS`). **The model:** talk to the coworker who owns the deliverable; they gather from teammates.

**Coworker-chat `@`-mentions + file attach** (`components/workers/worker-mention-input.tsx` = the ONE composer used in BOTH the in-thread chat and the home/briefing box; `/api/workers/mentions` source): `@` → **Coworkers / Tasks / Documents** picker (prefetched + cached on open so drilling is instant; clicking Mention again / away strips the dangling `@`). On send, `worker-chat-tab` posts `mentions[]`; resolved in `buildMentionContext` (+ the AgentOS bridge path appends the same context to the message): **@Coworker** → build on that teammate's work (`find_team_work` scoped), **@Task** → injects the task's latest output, **@Document** → a **knowledge-base file** (`knowledge_files` — meetings/uploads/generated; injected from `knowledge_chunks`), NOT the heavy `work_threads.artifacts` blob (fixed both coverage + a slow picker). **Attach** (📎) uploads via `/api/work/threads/[id]/chat-attach` (Supabase `email-attachments` + `text-extractor.ts` → `work_threads.user_attachments`; the bridge appends extracted text — **AgentOS path is text-only**, no image/scanned-PDF vision); **~4 MB client guard** (Vercel request-body limit — bigger → upload to Drive + `@mention`). The home box **buffers** files and uploads them on send (so the message shows instantly, no skeleton). Mention/attachment **chips render on sent bubbles** (`ChatMessageBubble` has coworker/task/document icon+colors; mentions hydrate from saved metadata on reload). The two composer host components (`WorkerHomeView` briefing landing + `ActiveWorkerChat` in-thread) **share `worker-mention-input`** — the single-persistent-composer rewrite is **deferred** (see memory `project_worker_composer_refactor.md`: 200+ line rewrite of delicate streaming/caching, no automated test).

### Drive (knowledge base) — uploads, indexing & search

Files live in `knowledge_files` + `knowledge_chunks` (pgvector); meetings (`provider_file_id` = `transcript::…`), uploads, and generated docs all index here. **Upload** = `/api/drive/upload/presign` (**25 MB**, direct to Supabase storage) → `/api/drive/upload/confirm`, which **registers the file row immediately and indexes in the background** (`after()`, `maxDuration=300`) so a large PDF (≈200 chunk-summary calls) can't time out the request. ⚠️ The indexer upserts a **`content_hash`** column — migration `20260611_knowledge_files_content_hash.sql` was never applied and is the real cause of "Failed to index file"; **apply it**. **Drive list** (`/api/drive/augmtd-files`) returns artifact **metadata only** via the `drive_augmtd_artifacts()` RPC (`supabase/migrations/20260621_drive_augmtd_artifacts.sql`) instead of shipping full document bodies — falls back to a JS flatten if the RPC is absent. **Search** = client filename substring (instant) **+ semantic content** (`/api/drive/search` → `searchKnowledgeGrouped`, debounced, merged). Chat KB retrieval = `search_knowledge_base` / `buildKBContext` (semantic, threshold 0.2). Chat `/chat-attach` uploads are direct multipart → **~4 MB Vercel body limit** (Drive's presign path is how big files get in).

### Coworker email (Resend, OAuth-free)

Each coworker sends real email from its own address on the verified **`team.augmtd.ai`** domain via **Resend** — no user OAuth. `EMAIL_LOCAL_BY_ROLE` (registry) → `clara@`/`luca@`/`max@` (sofia@ retired Aug 14); **`sendCoworkerEmail`** (`lib/tools/coworker-email.ts`) sends from `{Name} · {First}'s assistant`, **Reply-To = the user's login email**, to any recipient (free-text to/cc, validated). Per-coworker gated — **default ON**, a Tools-tab toggle (`agent_tool_settings` provider `'email'`; `isToolEnabledForAgent(..., 'email', true)`). Daily send cap + audit via `email_sends` (`supabase/migrations/20260622_email_sends.sql`, apply manually). Distinct from the existing **`/send-email`** route (connected Gmail/Outlook, sends AS the user — for inbox email artifacts).

- **Chat = user-confirmed (the model NEVER sends):** `compose_email` DRAFTS → streams an `email_draft` (native loop) or a `[[email_draft:<base64>]]` marker the AgentOS bridge decodes → an inline **editable `EmailDraftCard`** (To/Cc chips + free-text, Subject, Body) → the user edits and clicks **Send** → `POST /api/work/threads/[id]/send-coworker-email` → `sendCoworkerEmail`. Drafts persist on the assistant message metadata (`email_drafts[]`, each with an `id`); the send marks `sent_at` so it stays "sent" on reload. `[YOUR EMAIL ADDRESSES]` context (login + connected) resolves "me"/"us".
- **Task output = no confirm:** `home='email'` sends the deliverable **as the task's coworker** to `output_config.email_to[]`/`email_cc[]` (free-text) + connected-mailbox addresses, defaulting to the user's login email (so "briefing → me" needs zero setup). `run-workflow` email branch → `sendCoworkerEmail`. Builder `OutputEditor` free-text recipients field (no inbox needed); chat `update_task` `output_email_to`.
- **Presentation:** from-name = `{Name} · {First}'s assistant`; every email ends with a **signature** — the coworker **avatar** (loaded from `https://app.augmtd.ai/workers/{role}.png`, i.e. `public/workers/*.png` — so changing a coworker avatar means committing + deploying that file), name, "{role} to {user full name}", and the coworker's address (`personalEmailHtml`/`signatureHtml` in `coworker-email.ts`). Body has no max-width (flows to the client). The Gmail **sender-circle avatar** needs domain-level **BIMI** (one logo for the whole domain, can't be per-coworker) — not pursued.
- **Deliverability:** the shared `team.augmtd.ai` reputation is the operational risk — per-account cap + monitoring now, per-company subdomains later. **Inbound (replies → coworker) is NOT built** — the same deferred two-way phase as Slack inbound.

### Workspace feature gating (scalable — one map, all surfaces)

`lib/workspace/tool-capabilities.ts` is the **single source of truth** mapping each tool/step → its required workspace feature (`TOOL_FEATURE`, `null` = always on). Workspace features (`email, meetings, drive, agents, studio` — `lib/workspace/types.ts`) are set per-company in platform-admin. Every surface reads the map so they can't drift:
- **Coworker chat (native):** `buildChatTools` drops disabled tools; the `[TOOLS]` prompt only lists what's on (both gated on the early-loaded `features`). Executors keep a `ctx.features` backstop.
- **AgentOS path:** the internal tools route (`app/api/internal/agentos/tools/route.ts`) gates via `getWorkspaceFeatures` + the map (returns "unavailable" for off-feature tools) — Python tools stay static, no box redeploy when flags change.
- **Studio builder:** the tool picker disables (greys + tooltip, doesn't hide) gated steps via `GET /api/workspace/features`.
- **generate-config:** tells the model which tools are off so auto-built tasks skip them.

**Adding a tool/integration = one line in the map.** Adding a feature = a key in `WorkspaceFeatures` + mapping its tools. Platform-admin feature labels read **Coworkers** (`agents`) / **Tasks** (`studio`) — current product naming; underlying keys unchanged.

### Workers UI — team home (review desk)

`/workers` lands on a **team home** (`components/workers/team-home-view.tsx`) before any worker is selected — a cross-coworker "review desk". `GET /api/workers/home` aggregates recent deliverables (Ready for you), recent task runs (Recently, attributed), and upcoming runs (Coming up). A conversational AI team briefing streams from `POST /api/workers/team-briefing` (the team analogue of the per-worker `/api/workers/[id]/briefing`) — grounded in real data, distinguishing scheduled vs. user-asked, cached per user in `profiles.team_briefing` (regenerated only when there's newer activity). A "Your team" coworker card grid at the bottom fills the page when activity is sparse — each card opens that coworker's chat.

The roster left panel: a **Home / Skills segmented switcher** at the top (view switcher, not stacked pills), then a "Your team" label with a **cog** (manage workers), then the worker list. Nothing sits at the bottom. `?worker=`/`?thread=` deep-links still go straight to a worker.

### Worker skills (reusable "how to" instructions)

A **skill** is a curated, reusable prompt block describing *how* to handle a kind of work — a method, process, format, structure, or style (not only voice/tone). Distinct from tasks (*what/when*), KB (searchable *documents*), and memory (passively *learned*). Skills are user-owned (team-level library) and assigned to specific workers.

- **Schema** (`supabase/migrations/20260618_skills.sql` — apply manually): `skills` (user-owned: `name`, `when_to_use`, `content`, `source`, `company_id` reserved for future team sharing) + `agent_skills` join (mirrors `agent_knowledge_sources`). Both owner-RLS.
- **Library UI**: `components/workers/skills-library-view.tsx` — reached via the roster's **Skills** segment. Card grid with create/edit modal, inline delete, assigned-worker avatars + an in-place **assign popover** per card. `.md` import (Claude SKILL.md style — `lib/skills/markdown.ts` parses YAML frontmatter `name`/`description` + body) and `.md` export round-trip; DB row is the system of record, `.md` is the interchange format.
- **APIs**: `GET/POST /api/skills`, `PATCH/DELETE /api/skills/[id]`, `GET/PUT /api/agents/[id]/skills` (replace a worker's assignments), `POST /api/skills/[id]/assign` (toggle one skill↔worker, used by library cards).
- **Assignment UI**: a Skills section in each worker's Knowledge tab (`worker-knowledge-tab.tsx`) — checklist of the library, optimistic toggle → PUT.
- **Chat injection (smart-auto)**: `lib/work/worker-skills-context.ts` → `buildSkillsBlock(client, agentId)` renders a `[SKILLS — reusable instructions for how to handle specific kinds of work…]` block; each skill tagged with its `when_to_use` hint so the worker picks the right one per task (no per-conversation picking). Injected in **both** worker-chat run paths: the AgentOS bridge (`buildWorkerRunContext`, live prod path) and the native loop (`chat/route.ts`). Rides the existing `dependencies.user_context` channel.
- **Skills in tasks (enforced)**: tasks are pipelines of `tool`+`ai` steps (generate-config emits **no `agent` steps**), so the worker's voice — and skills — apply on the **last `ai` step** via `executeAIStep` in `lib/workflows/execute-step.ts`, NOT the AgentOS path. The worker branch injects skills there too: if the workflow has pinned `skill_ids` → `buildSkillsBlockByIds(client, userId, skill_ids)`; else → `buildSkillsBlock` (the worker's assigned skills, parity with chat). Threaded via `StepContext.skillIds` ← `run-workflow.ts` ← `workflows.skill_ids` (loaded by `runWorkflow`'s `select('*')`). **Pure TS — no AgentOS redeploy for task skills.**
- **Per-task skill selector** (`workflows.skill_ids JSONB` — `supabase/migrations/20260619_workflows_skill_ids.sql`, apply manually): "Skills to apply" chip selector in the New task modal (`worker-tasks-tab.tsx` — assigned pre-selected; stores `[]` when unchanged so the task *follows* assignments, pins explicitly when customized; skills prefetched by the parent tab to avoid pop-in) and the studio builder (`studio-builder.tsx` IdentitySection). Persisted via workflows `POST`/`PATCH`; empty `skill_ids` → fall back to assigned.
- **Chat tools — library on demand + task pinning** (`lib/tools/worker-skills.ts`): `list_skills` (full library, marks assigned) + `apply_skill(skill_name)` (pull one skill by name — exact then fuzzy — even unassigned, for the current response). Plus name-based pinning: `create_task`/`update_task` accept `skill_names` (resolved → `skill_ids` via `resolveSkillIdsByName`/`normalizeSkillNames`), so *"set up a weekly brief using my Exec summary skill"* pins it. Wired into native loop (`chat/route.ts` dispatch + `[SKILLS]` prompt note), AgentOS internal route (`app/api/internal/agentos/tasks/route.ts`), and Python `@tool`s in `infra/agentos/tools_tasks.py`. **AgentOS-path tools (`list_skills`/`apply_skill` + `skill_names` on the Python task tools) require an AgentOS redeploy** (Python baked into image); native path + all task execution + the selector are live on Vercel deploy.

### Company Admin — AI Operations dashboard (v1 + Phase 2, July 2026)

`Settings → Company → AI Operations` (admin/owner-only — gated in nav, server render, AND API routes; three independent layers). `lib/company/ai-operations-metrics.ts` `getAIOperationsSummary` aggregates across every active company member (member ids → `.in()` with the service-role client): runs, adoption, per-coworker breakdown (runs + chat messages + emails + top tools — chat counts too, from `work_messages.metadata.tool_calls`), hours-saved estimate, ROI banner.

- **Real cost tracking**: `lib/ai/log-usage.ts` `logAIUsage()` (non-fatal, fire-and-forget) → `ai_usage_events`; `AIUsageSource` in that file is the definitive instrumented-call-site list (14+ sources). `lib/ai/pricing.ts` static per-model price table (fallback + warn on unknown).
- ⚠️ **Tier vs task-type lesson**: `ResolvedClient` now exposes the real billing `tier: TierType`; a cost column meant to slice by billing tier must not receive the AI TaskType — both are plain strings, easy to conflate. Separate `task_type` column holds the task dimension.
- **AgentOS telemetry needed NO Python change**: Agno already returns token metrics on `RunOutput.metrics` / the `RunCompleted` SSE frame — the bridge just wasn't reading them. Before assuming a cross-service integration needs a redeploy for telemetry, check whether the data is already in the payload and unread. (`tier: 'bedrock_optimised'` hardcoded — AgentOS only ever runs that tier.)
- **Hours-saved methodology (honest, not measured)**: 15 min × completed runs, ONLY for "grounded" runs (≥1 tool/agent step); ai-only tasks are "insight only". Documented limitation (deferred): a fetch-to-personalize-advice task still counts as grounded — the real signal is the final AI step's prompt intent (semantic), would need a cached per-workflow classification, not builder tagging.
- **Privacy model**: `usageDistribution` is anonymized stable slots (same person = same color across coworkers, never named); `costByUser` shows REAL member names by default (admins already see them in Members) with an "Anonymize" toggle for screenshots; slot assignment unions run-ranked users with cost-only users (chat-only users used to collide in slot 0).
- **Perf lesson**: `getUserNames()` was serialized AFTER the main Promise.all while its actual input (`memberIds`) was known upfront — a call that "logically depends" on another result belongs in the same parallel batch when its real inputs are already known.
- Editable blended €/hr in `companies.settings.blended_hourly_rate_eur` (PATCH, range-validated); EUR/USD display toggle (fixed rate, display-only); live-poll on focus + 2-min interval (cheap aggregation, safe to poll); in/out token split via `fmtTokens`; breakdowns collapse to top-5.

### Company Admin — Strategy tab (goals + AI recommendations, July 8–14)

`Settings → Company → Strategy` (admin-only, same three-layer gating). **HARD INVARIANT (by design, not convention): company goals NEVER reach any employee-facing coworker context** — no `buildWorkerRunContext`, no `dependencies.user_context`, no workflow step reads `company_goals`. Strategy is a read-only admin lens; employees keep full agency over their coworkers.

- **Schema**: `company_goals` (kind `north_star|goal`, status, sort_order; RLS via `is_company_admin`). **North Star is positional** — dragging to index 0 IS promotion; `PATCH /api/company/goals/reorder` takes the full ordered list and derives kind+sort_order server-side (frontend never sets kind).
- **Recommendations (reframed from "Alignment")**: `lib/company/synthesize-alignment.ts` produces 1–3 concrete day-to-day suggestions per goal (suggestion is the headline; tone = "Double down / Course correct / New idea" as a secondary badge). ⚠️ **Actor lesson**: a recommendation aimed at an admin must be ORGANIZATIONAL (rollout, adoption gap, team expectation) — each coworker is a per-user instance; the admin doesn't personally operate Clara. The prompt states this + feeds real adoption numbers.
- **Cache**: 12h TTL keyed to a sig (period + goals' id:updated_at + activity counts + **`ALIGNMENT_PROMPT_VERSION`**) in `companies.settings.alignment_cache`; re-runs only on Strategy-tab load with changed sig; proactive refetch (visible loading) after any goal mutation/promotion; plain reorders deliberately don't re-trigger. ⚠️ **Any prompt-driven cache needs the prompt's own version in its invalidation key.** Includes a spend-concentration line from AIOperationsSummary so unmapped heavy spend can surface as drift.
- **Client cache**: module-level goals/alignment caches + localStorage — but ⚠️ on an SSR'd route the cache read lives in `useLayoutEffect`, NEVER a useState initializer (server/client first-paint diverge → hydration mismatch when warm). Mutations write through; `invalidateAlignmentCache()` clears every period.
- UI reuses established patterns (Bars2Icon drag handle, two-step inline archive confirm mirroring the skills library, h-full cards); archive = soft delete (status archived, no restore UI yet). `SegmentedControl` transition smoothed app-wide.

### Shared UI kit (`components/ui/`)

Dependency-free primitive layer that enforces one consistent look. Import from `@/components/ui`; prefer over inline Tailwind for these elements. See `components/ui/README.md` for full tokens.
- **Components**: `Button` (variant primary|secondary|soft|ghost|danger × size sm|md), `IconButton` (tone default|danger), `Badge` (tone neutral|indigo|emerald|amber|red|blue), `Input`/`Textarea`/`Select`, `Panel` (column wrapper) / `Card` (`interactive` hover), `SegmentedControl` (view switcher; `value` accepts `null` for no active segment) / `TabBar` (in-panel section tabs), `EmptyState`. `lib/cn.ts` = tiny classnames joiner (no clsx/tailwind-merge — don't override component padding/size via className, use the size variant).
- **Tokens**: indigo accent (`indigo-600` primary, `indigo-50`/`indigo-700` active, `border-indigo-300` focus — no rings; never `bg-primary-*`). Radii: controls/inputs `rounded-lg`, cards `rounded-xl`, panels/modals `rounded-2xl`, pills `rounded-full`. Font scale: `text-[11px]` labels · `text-[12px]` small controls · `text-[13px]` body/controls · `text-[15px]` card titles · `text-[18px]` section headers · `text-[24px]` page title.
- **Adoption**: workers/skills, inbox, meetings, drive, settings converted. Intentionally bespoke (left as-is): chat/meeting composers, note title/body editors, meeting recorder controls, calendar day cells, rich per-type color maps (`TypeBadge`/`SourceBadge`, inbox role pills). Peripheral marketing/auth pages (signup, privacy, terms, onboarding, platform-admin) still use brand violet.

### Meetings / recording pipeline

Recording path for in-person audio:
1. `hooks/useRecording.ts` — browser MediaRecorder, WebM/Opus
2. `app/api/meetings/recordings/presign` → signed Supabase Storage URL
3. `app/api/meetings/recordings/confirm` → inserts `meeting_transcripts` row with `bot_state='processing'`, fires POST to Hetzner `/transcribe`
4. `infra/meeting-bot/transcription_worker.py` — downloads audio, ffmpeg remux, calls local Whisper, writes segments to DB, calls `/api/meetings/recording/[id]/generate-insights`
5. `generate-insights` route → `lib/integrations/meeting-bot/bot-manager.ts` → `storeTranscriptAndGenerateWork()` → AI insights, action items in `inbox_items`, sets `processed=true, bot_state='ended'`

The meeting bot (`bot_runner.py`) uses Playwright + PulseAudio to join Google Meet, scrape captions, and record audio. Bot is currently disabled for users (`DEFAULT_FEATURES.meetings = false`); in-person recording remains active.

**Recording pause behaviour:** tab/app switches do not pause. Laptop sleep/screen lock auto-pauses via the Page Lifecycle `freeze` event (Chrome/Edge — immediate) or a `visibilitychange` + **1-hour** timer (all browsers — universal safety net). `awaySeconds` counter ticks while tab is hidden; document title shows countdown warnings at 45 min and 55 min away. Pre-recording hint near "Record in person" button reads "may pause if screen locks or 1h away". `transcription_worker.py` retries the Whisper POST up to 3× on connection errors (cold-start resilience) and uses a 300s timeout for the generate-insights callback (matching Vercel's `maxDuration`).

### Context profiles (user memory)

`context_profiles` table stores modular learned knowledge about each user: `identity`, `communication_style`, `domain_knowledge`, `relationships`, `work_patterns`. Each row has a `confidence` float (0–100) and `data` JSONB.

Profiles are read-only from the UI — they update automatically from usage signals via `lib/context/`. `lib/context/render-memory.ts` generates human-readable prose from raw profile data on demand.

### Meeting note sharing

`meeting_transcripts` has two sharing columns: `sharing_mode TEXT ('live'|'specific'|NULL)` and `company_id UUID`. `shared_note_receipts` is a join table — dual purpose: (1) access control list for `specific` mode, (2) per-recipient folder assignment.

- **`live`** — all company members can read
- **`specific`** — only users with a `shared_note_receipts` row can read
- `sharing_mode = NULL` — private (default)

Key APIs:
- `PATCH /api/meetings/[id]/sharing` — owner toggles mode + syncs receipt rows
- `GET /api/meetings/teammates` — returns company members (two-query pattern: fetch `company_members` user_ids first, then join `profiles` separately — FK join alias is unreliable)
- `GET /api/meetings/[id]/full` — returns `isOwner: bool` + `sharedByName`; shared access fallback tries `live` then `specific`
- `PATCH /api/meetings/recording/[id]/folder` — routes to `meeting_transcripts.folder_id` for owners or `shared_note_receipts.folder_id` for recipients

Recipients see notes read-only: `MeetingDocument` gets `editable={isOwner}`, plain-text textarea gated by `{isOwner && ...}`, no delete/share/record controls, AI chat `UPDATE_MEETING` blocked (transcriptId omitted from context).

Migrations applied: `20260613_meeting_transcripts_sharing.sql`, `20260613b_sharing_mode_specific.sql`.

### Key data model relationships

```
connections (email accounts) → emails → inbox_items
                                      ↳ learning_signals (feedback loop)
users → context_profiles
      → workflows → workflow_runs → work_threads → work_messages
      → custom_agents
      → meeting_transcripts → transcript_segments
                            ↳ shared_note_receipts (sharing access + per-recipient folder)
      → calendar_events
      → knowledge_sources → kb_files (Drive + uploads)
```

### Path conventions

- `app/api/**` — all API routes. Route files export named HTTP method handlers (`GET`, `POST`, etc.)
- `app/(main)/**` — authenticated app pages. Layout at `app/(main)/layout.tsx` gates auth via middleware
- `components/` — UI components, grouped by feature (`inbox/`, `meetings/`, `work/`)
- `lib/` — all business logic. Never import from `app/` into `lib/`
- `context/` — React context providers (meetings data, recording state, workspace)
- `hooks/` — client-side hooks (`useRecording`, etc.)
- `infra/` — Hetzner deployment files (Python, Docker)
- `supabase/migrations/` — SQL migration files (apply manually)

### Environment variables (key ones)

| Variable | Purpose |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase project URL |
| `SUPABASE_SERVICE_ROLE_KEY` | Bypasses RLS — server only |
| `OPENAI_API_KEY` | Standard tier OpenAI |
| `ANTHROPIC_API_KEY` | Standard tier Anthropic |
| `AWS_BEDROCK_*` | Bedrock credentials |
| `MEETING_BOT_SERVICE_URL` | Hetzner bot service (`http://46.224.176.245:3001`) |
| `MEETING_BOT_SECRET` | Bearer token for Hetzner ↔ Vercel calls |
| `WHISPER_SERVICE_URL` | Whisper endpoint (`http://172.19.0.1:8000` inside bot container) |
| `AUGMTD_WEBHOOK_BASE_URL` | Vercel production URL for Hetzner callbacks |
| `RESEND_API_KEY` | Email digest notifications |
| `TAVILY_API_KEY` | Web search in deep_research tool |
| `NANGO_HOST` | Self-hosted Nango (`https://nango.augmtd.ai`) — integrations/OAuth |
| `NANGO_SECRET_KEY` | Nango **Prod** API key (account `alex@augmtd.ai`, env 3) — server only |
