// The @modal parallel slot renders nothing by default — only the intercepting route
// ((.)item/[id]) fills it, on a soft-navigation from the Home.
export default function ModalDefault() {
  return null;
}
